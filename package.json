{
  "name": "@repo/root",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc --build",
    "build:clean": "tsc --build --clean",
    "dev": "npm run dev --workspace=@repo/server",
    "test": "npm run test --workspaces --if-present",
    "lint": "echo 'Linting not configured yet'",
    "format": "echo 'Formatting not configured yet'",
    "typecheck": "tsc --build --dry"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
