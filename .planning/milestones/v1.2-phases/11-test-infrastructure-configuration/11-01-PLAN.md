---
phase: 11-test-infrastructure-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - .github/workflows/playwright.yml
  - package.json
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "Playwright config defines 9 browser/viewport project combinations (3 browsers x 3 viewports)"
    - "Tests can run against Docker production container via environment variable"
    - "CI workflow installs all three browsers (Chromium, Firefox, WebKit)"
    - "npm scripts exist for both dev-server and Docker-targeted test runs"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Cross-browser, cross-device, Docker-aware Playwright configuration"
      contains: "firefox"
    - path: ".github/workflows/playwright.yml"
      provides: "CI pipeline installing all browsers"
      contains: "firefox webkit"
    - path: "package.json"
      provides: "E2E npm scripts for dev and Docker modes"
      contains: "test:e2e:docker"
  key_links:
    - from: "playwright.config.ts"
      to: "docker-compose.yml"
      via: "E2E_BASE_URL environment variable"
      pattern: "E2E_BASE_URL|process\\.env"
    - from: ".github/workflows/playwright.yml"
      to: "playwright.config.ts"
      via: "npx playwright install"
      pattern: "playwright install.*firefox.*webkit|playwright install --with-deps"
---

<objective>
Configure Playwright for cross-browser (Chromium, Firefox, WebKit), cross-device (desktop, tablet, mobile) testing with the ability to target either the dev server or Docker production container.

Purpose: Foundation for comprehensive E2E testing across all browser/device combinations and against production builds.
Output: Updated playwright.config.ts, CI workflow, and npm scripts.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@playwright.config.ts
@.github/workflows/playwright.yml
@package.json
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Playwright config with 9 browser/viewport projects and Docker support</name>
  <files>playwright.config.ts</files>
  <action>
Rewrite playwright.config.ts to define 9 projects (3 browsers x 3 viewports):

1. `desktop-chromium` — devices['Desktop Chrome'], viewport 1280x720
2. `desktop-firefox` — devices['Desktop Firefox'], viewport 1280x720
3. `desktop-webkit` — devices['Desktop Safari'], viewport 1280x720
4. `tablet-chromium` — browserName: 'chromium', viewport 768x1024, isMobile: false
5. `tablet-firefox` — browserName: 'firefox', viewport 768x1024, isMobile: false
6. `tablet-webkit` — browserName: 'webkit', viewport 768x1024, isMobile: false
7. `mobile-chromium` — devices['iPhone 13 Pro'] (375x812), keep browserName: 'chromium'
8. `mobile-firefox` — browserName: 'firefox', viewport 375x812, isMobile: true, hasTouch: true
9. `mobile-webkit` — devices['iPhone 13 Pro'] (375x812, uses WebKit natively)

For the baseURL, read from `process.env.E2E_BASE_URL || 'http://localhost:3000'` so tests can target Docker.

For the webServer section, only start the dev server when `E2E_BASE_URL` is NOT set (i.e., not targeting Docker):
```typescript
webServer: process.env.E2E_BASE_URL ? undefined : {
  command: 'npm run dev',
  url: 'http://localhost:3000',
  reuseExistingServer: !process.env.CI,
  timeout: 120_000,
},
```

Keep all existing settings: fullyParallel, forbidOnly on CI, retries 2 on CI, workers 1 on CI, html+list reporters, trace on-first-retry, screenshot only-on-failure.

IMPORTANT: The old project names were "desktop" and "mobile". The existing tests use `isMobile` fixture which Playwright provides based on project config. Ensure the mobile projects set `isMobile: true` and tablet projects set `isMobile: false` so existing test logic (e.g., `test.skip(({ isMobile }) => isMobile)`) continues to work correctly.

Add a comment block at the top explaining the project naming convention: `{viewport}-{browser}`.
  </action>
  <verify>
Run `npx playwright test --list` to verify all 9 projects are recognized and tests are listed without errors. Confirm project names appear in the output. Do NOT run actual tests (they may fail due to missing browsers — that's fine).
  </verify>
  <done>
playwright.config.ts defines 9 projects covering 3 browsers x 3 viewports, baseURL reads from E2E_BASE_URL env var, webServer conditionally starts only when not targeting Docker.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CI workflow and add Docker E2E npm scripts</name>
  <files>.github/workflows/playwright.yml, package.json</files>
  <action>
**CI Workflow (.github/workflows/playwright.yml):**

Update the "Install Playwright browsers" step to install all three browsers:
```yaml
- name: Install Playwright browsers
  run: npx playwright install --with-deps chromium firefox webkit
```

Keep everything else the same (the CI still runs against dev server by default for now — Docker CI integration is a future concern, not Phase 11 scope).

**package.json scripts:**

Add these npm scripts alongside existing ones:
- `"test:e2e:docker"` — `"E2E_BASE_URL=http://localhost:3000 playwright test"` (runs all tests against Docker container, user must start Docker first)
- `"test:e2e:docker:up"` — `"docker compose up -d --build --wait && npm run test:e2e:docker; docker compose down"` (builds, starts Docker, runs tests, tears down)
- `"test:e2e:chromium"` — `"playwright test --project='desktop-chromium' --project='tablet-chromium' --project='mobile-chromium'"` (quick: Chromium-only across viewports)

Keep all existing scripts unchanged (`test:e2e`, `test:e2e:ui`, `test:e2e:report`).
  </action>
  <verify>
1. Verify `npx playwright install --with-deps chromium firefox webkit` succeeds (install all browsers locally).
2. Verify `npm run test:e2e:chromium -- --list` lists tests for 3 Chromium projects.
3. Run `npm run test:e2e:chromium` to confirm existing tests pass on all 3 Chromium viewport projects (this validates backward compatibility).
  </verify>
  <done>
CI installs all 3 browsers, npm scripts exist for Docker-targeted and browser-filtered test runs, existing tests pass on Chromium across all 3 viewports.
  </done>
</task>

</tasks>

<verification>
1. `npx playwright test --list` shows tests across 9 projects
2. `npm run test:e2e:chromium` passes (existing tests work on desktop/tablet/mobile Chromium)
3. `.github/workflows/playwright.yml` installs chromium, firefox, webkit
4. `E2E_BASE_URL` env var controls dev-server vs Docker targeting
</verification>

<success_criteria>
- 9 Playwright projects defined (3 browsers x 3 viewports)
- Existing 72 tests still pass on Chromium projects
- Docker targeting works via E2E_BASE_URL environment variable
- CI workflow installs all browsers
- npm scripts for Docker and Chromium-only testing exist
</success_criteria>

<output>
After completion, create `.planning/phases/11-test-infrastructure-configuration/11-01-SUMMARY.md`
</output>
