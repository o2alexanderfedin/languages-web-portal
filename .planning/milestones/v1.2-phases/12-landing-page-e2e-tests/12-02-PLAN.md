---
phase: 12-landing-page-e2e-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/tests/landing-interactions.spec.ts
autonomous: true
requirements: [LAND-04]

must_haves:
  truths:
    - "Try Now buttons for available and in-development tools navigate to /demo?tool={id}"
    - "Coming Soon tool buttons are disabled and do not navigate"
    - "Navigation works correctly on both desktop table rows and mobile cards"
  artifacts:
    - path: "e2e/tests/landing-interactions.spec.ts"
      provides: "Landing page interaction and navigation E2E tests"
      min_lines: 60
  key_links:
    - from: "e2e/tests/landing-interactions.spec.ts"
      to: "e2e/pages/LandingPage.ts"
      via: "POM import"
      pattern: "import.*LandingPage"
    - from: "e2e/tests/landing-interactions.spec.ts"
      to: "packages/client/src/features/landing/ToolComparisonGrid.tsx"
      via: "data-testid selectors for Try Now buttons"
      pattern: "tool-try-now"
---

<objective>
Create E2E tests verifying Try Now button navigation for available/in-development tools and disabled state for Coming Soon tools across all 9 Playwright projects.

Purpose: Satisfy LAND-04 (Try Now navigation + Coming Soon disabled state).
Output: New landing-interactions.spec.ts test file.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@e2e/pages/LandingPage.ts
@e2e/tests/landing-navigation.spec.ts
@packages/client/src/features/landing/ToolComparisonGrid.tsx
@packages/shared/src/constants/tools.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create landing-interactions.spec.ts with Try Now and Coming Soon tests</name>
  <files>e2e/tests/landing-interactions.spec.ts</files>
  <action>
Create `e2e/tests/landing-interactions.spec.ts` with these tests. Use `isMobile` fixture. Import LandingPage POM.

**Test describe: "Landing Page Interactions"**

All tests use `test.beforeEach` to navigate: `const landing = new LandingPage(page); await landing.goto();`

1. **"Try Now button navigates to demo page for available tool (java-verification)"**
   - Get the Try Now button: if `!isMobile`, locate within `landing.getToolRow('java-verification')`; if `isMobile`, locate within `landing.getToolCard('java-verification')`. Use `page.getByTestId('tool-try-now-java-verification')`.
   - Assert button is enabled (not disabled)
   - Click the button
   - `await page.waitForURL(/\/demo\?tool=java-verification/)`
   - Assert URL contains `demo?tool=java-verification`

2. **"Try Now button navigates to demo page for in-development tool (cpp-to-c-transpiler)"**
   - Same pattern as test 1 but with `cpp-to-c-transpiler`
   - Get Try Now button from appropriate row/card
   - Assert button is enabled
   - Click and verify navigation to `/demo?tool=cpp-to-c-transpiler`

3. **"Coming Soon tools have disabled Try Now buttons"**
   - Define coming-soon tool IDs: `['python-linter', 'typescript-linter', 'bash-verification']`
   - For each tool ID:
     - Get Try Now button via `page.getByTestId('tool-try-now-{toolId}')` — the button exists in both table and cards but only one is visible per viewport. Use the appropriate container:
       - If `!isMobile`: `landing.getToolRow(toolId).getByTestId('tool-try-now-' + toolId)`
       - If `isMobile`: `landing.getToolCard(toolId).getByTestId('tool-try-now-' + toolId)`
     - Assert button is disabled: `await expect(button).toBeDisabled()`

4. **"Clicking disabled Coming Soon button does not navigate"**
   - Pick one coming-soon tool: `python-linter`
   - Get the Try Now button (from row or card based on isMobile)
   - Assert it is disabled
   - Record current URL: `const currentUrl = page.url()`
   - Force-click the button with `{ force: true }` to bypass disabled check (Playwright normally refuses to click disabled elements)
   - Wait briefly: `await page.waitForTimeout(500)`
   - Assert URL has not changed: `expect(page.url()).toBe(currentUrl)`

Use standard `import { test, expect } from '@playwright/test'` — no custom fixtures needed.
  </action>
  <verify>Run `npx playwright test e2e/tests/landing-interactions.spec.ts --project=desktop-chromium --project=mobile-chromium` and verify all tests pass. Then run across all 9 projects.</verify>
  <done>4 test cases in landing-interactions.spec.ts all pass across 9 Playwright projects (36 total test runs). Try Now navigation works for available/in-development tools; Coming Soon buttons confirmed disabled.</done>
</task>

<task type="auto">
  <name>Task 2: Verify full landing page test suite across all 9 projects</name>
  <files>e2e/tests/landing-content.spec.ts, e2e/tests/landing-interactions.spec.ts</files>
  <action>
Run the complete set of new landing page tests across all 9 Playwright projects to confirm cross-browser/cross-viewport coverage.

1. Run: `npx playwright test e2e/tests/landing-content.spec.ts e2e/tests/landing-interactions.spec.ts`
2. Verify all 9 projects produce passing results
3. If any failures:
   - Diagnose browser-specific issues (Firefox text rendering, WebKit click behavior, etc.)
   - Fix the test code to handle cross-browser differences
   - Re-run until all pass
4. Run existing landing tests to ensure no regressions: `npx playwright test e2e/tests/landing-navigation.spec.ts e2e/tests/responsive-layout.spec.ts e2e/tests/java-fv-landing.spec.ts`

Do NOT modify the existing test files (landing-navigation.spec.ts, responsive-layout.spec.ts, java-fv-landing.spec.ts) — they provide backward compatibility. Only fix the new test files if issues arise.
  </action>
  <verify>`npx playwright test e2e/tests/landing-content.spec.ts e2e/tests/landing-interactions.spec.ts` shows all passing across 9 projects. Existing landing tests also pass.</verify>
  <done>8 new test cases x 9 projects = 72 test runs all passing. No regressions in existing tests. Full cross-browser landing page coverage achieved.</done>
</task>

</tasks>

<verification>
- `npx playwright test e2e/tests/landing-interactions.spec.ts` passes on all 9 projects
- Try Now navigates to /demo?tool={id} for java-verification and cpp-to-c-transpiler
- Coming Soon buttons (python-linter, typescript-linter, bash-verification) are disabled
- Disabled buttons do not cause navigation
- Existing landing tests (landing-navigation, responsive-layout, java-fv-landing) still pass
</verification>

<success_criteria>
- 4 test cases x 9 projects = 36 test executions, all passing
- LAND-04: Try Now navigation verified for available/in-development tools; Coming Soon disabled state verified
- No regressions in existing test suites
</success_criteria>

<output>
After completion, create `.planning/phases/12-landing-page-e2e-tests/12-02-SUMMARY.md`
</output>
