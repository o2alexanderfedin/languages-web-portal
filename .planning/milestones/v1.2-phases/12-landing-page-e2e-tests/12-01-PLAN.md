---
phase: 12-landing-page-e2e-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/pages/LandingPage.ts
  - e2e/tests/landing-content.spec.ts
autonomous: true
requirements: [LAND-01, LAND-02, LAND-03]

must_haves:
  truths:
    - "Hero section renders with headline and mission statement on all 9 browser/viewport projects"
    - "All 8 tools display in the comparison grid with correct status badges"
    - "Desktop viewports show table layout; mobile viewports show card layout"
    - "Tablet viewports show table layout (md breakpoint = 768px, tablet is 768px wide)"
  artifacts:
    - path: "e2e/tests/landing-content.spec.ts"
      provides: "Comprehensive landing page content and layout E2E tests"
      min_lines: 80
    - path: "e2e/pages/LandingPage.ts"
      provides: "Enhanced POM with mission statement and tool count locators"
      exports: ["LandingPage"]
  key_links:
    - from: "e2e/tests/landing-content.spec.ts"
      to: "e2e/pages/LandingPage.ts"
      via: "POM import"
      pattern: "import.*LandingPage"
    - from: "e2e/tests/landing-content.spec.ts"
      to: "packages/client/src/pages/Landing.tsx"
      via: "data-testid selectors"
      pattern: "getByTestId"
---

<objective>
Create comprehensive E2E tests verifying landing page content and responsive layout across all 9 Playwright projects (3 browsers x 3 viewports).

Purpose: Satisfy LAND-01 (hero/mission/CTA), LAND-02 (all 8 tools with status badges), and LAND-03 (responsive table vs cards layout switching).
Output: Enhanced LandingPage POM and new landing-content.spec.ts test file.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-test-infrastructure-configuration/11-01-SUMMARY.md

@e2e/pages/LandingPage.ts
@e2e/tests/responsive-layout.spec.ts
@e2e/tests/landing-navigation.spec.ts
@packages/client/src/pages/Landing.tsx
@packages/client/src/features/landing/HeroSection.tsx
@packages/client/src/features/landing/ToolComparisonGrid.tsx
@packages/shared/src/constants/tools.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance LandingPage POM with mission statement and tool count locators</name>
  <files>e2e/pages/LandingPage.ts</files>
  <action>
Add the following locators to the LandingPage POM class:

1. `missionStatement` — Locator for the paragraph in hero section containing "vericoding" text. Use `this.heroSection.locator('p')` since there is exactly one paragraph in HeroSection.
2. `toolCount` method — `getToolRows()` returns `this.toolTable.locator('tbody tr')` for counting desktop rows; `getToolCardsList()` returns `this.toolCards.locator('[data-testid^="tool-card-"]')` for counting mobile cards.
3. `getToolStatusBadge(toolId: string, isMobile: boolean)` — Returns the status badge text locator within the appropriate row or card. For desktop: `this.getToolRow(toolId).locator('span.rounded-full')`. For mobile: `this.getToolCard(toolId).locator('span.rounded-full')`.

Keep all existing locators and methods unchanged. Only add new ones.
  </action>
  <verify>Run `npx tsc --noEmit -p e2e/tsconfig.json 2>/dev/null || npx tsc --noEmit` to verify no TypeScript errors. If there's no e2e tsconfig, just check the file compiles conceptually by reviewing imports.</verify>
  <done>LandingPage POM has missionStatement, getToolRows, getToolCardsList, and getToolStatusBadge members.</done>
</task>

<task type="auto">
  <name>Task 2: Create landing-content.spec.ts with hero, tool grid, and responsive layout tests</name>
  <files>e2e/tests/landing-content.spec.ts</files>
  <action>
Create `e2e/tests/landing-content.spec.ts` with these test groups. Use `isMobile` fixture from Playwright projects (do NOT use manual `page.setViewportSize`). Import LandingPage POM.

**Test describe: "Landing Page Content"**

All tests start with `const landing = new LandingPage(page); await landing.goto();`

1. **"hero section displays headline and mission statement"**
   - Assert `landing.heroSection` is visible
   - Assert `landing.heroTitle` is visible and contains text "Formal Verification for AI-Generated Code"
   - Assert `landing.missionStatement` is visible and contains text "vericoding"
   - Assert mission statement contains "96%" (the statistic)

2. **"tool comparison grid displays all 8 tools"**
   - Assert `landing.toolComparisonGrid` is visible
   - If `!isMobile`: assert `landing.getToolRows()` has count 8
   - If `isMobile`: assert `landing.getToolCardsList()` has count 8

3. **"each tool shows correct status badge"**
   Define expected statuses:
   ```typescript
   const expectedStatuses: Record<string, string> = {
     'cpp-to-c-transpiler': 'In Development',
     'cpp-to-rust-transpiler': 'In Development',
     'csharp-verification': 'In Development',
     'java-verification': 'Available',
     'rust-verification': 'In Development',
     'python-linter': 'Coming Soon',
     'typescript-linter': 'Coming Soon',
     'bash-verification': 'Coming Soon',
   };
   ```
   Loop over entries. For each tool:
   - If `!isMobile`: get `landing.getToolRow(toolId)`, assert visible, get badge via `.locator('span.rounded-full')`, assert badge has text matching expected status
   - If `isMobile`: get `landing.getToolCard(toolId)`, assert visible, get badge via `.locator('span.rounded-full')`, assert badge has text matching expected status

4. **"desktop/tablet shows table layout, mobile shows cards"**
   - If `!isMobile`: assert `landing.toolTable` is visible, assert `landing.toolCards` is NOT visible (uses CSS `md:hidden`)
   - If `isMobile`: assert `landing.toolCards` is visible, assert `landing.toolTable` is NOT visible (uses CSS `hidden md:block`)

Note: Per Playwright config, tablet projects have `isMobile: false`, so tablet correctly checks for table layout (tablets are 768px wide which hits the `md:` breakpoint).

Use standard `import { test, expect } from '@playwright/test'` — no custom fixtures needed.
  </action>
  <verify>Run `npx playwright test e2e/tests/landing-content.spec.ts --project=desktop-chromium --project=mobile-chromium` and verify all tests pass on both viewport types. Then run `npx playwright test e2e/tests/landing-content.spec.ts` to verify across all 9 projects.</verify>
  <done>4 test cases in landing-content.spec.ts all pass across 9 Playwright projects (36 total test runs). Hero, mission statement, all 8 tools with badges, and responsive layout verified.</done>
</task>

</tasks>

<verification>
- `npx playwright test e2e/tests/landing-content.spec.ts` passes on all 9 projects
- Hero section, mission text with "vericoding" and "96%" verified
- All 8 tools visible with correct status badges (Available/In Development/Coming Soon)
- Table visible on desktop/tablet, cards visible on mobile
</verification>

<success_criteria>
- 4 test cases x 9 projects = 36 test executions, all passing
- LAND-01: Hero headline + mission statement verified across browsers/viewports
- LAND-02: All 8 tools with accurate status badges verified
- LAND-03: Responsive table/cards layout switching verified
</success_criteria>

<output>
After completion, create `.planning/phases/12-landing-page-e2e-tests/12-01-SUMMARY.md`
</output>
