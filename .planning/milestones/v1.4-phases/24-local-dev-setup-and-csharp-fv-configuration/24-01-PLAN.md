---
phase: 24-local-dev-setup-and-csharp-fv-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/config/toolRegistry.ts
  - packages/server/.env
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04

must_haves:
  truths:
    - "CSHARP_FV_CMD env var, when set, changes which command the tool registry invokes for csharp-verification"
    - "When CSHARP_FV_CMD is unset, csharp-verification falls back to /usr/local/bin/hupyy-csharp-verify"
    - "packages/server/.env sets CSHARP_FV_CMD to the local wrapper script path"
    - "packages/server/.env sets CS_FV_DLL to the local cs-fv publish output path"
    - "packages/server/.env sets JAVA_HOME to jdk-21 (not jdk-22)"
    - "packages/server/.env sets CVC5_PATH to ~/bin/cvc5"
  artifacts:
    - path: "packages/server/src/config/toolRegistry.ts"
      provides: "env-driven csharp-verification command"
      contains: "process.env.CSHARP_FV_CMD"
    - path: "packages/server/.env"
      provides: "local environment variables for C# FV"
      contains: "CSHARP_FV_CMD"
  key_links:
    - from: "packages/server/.env"
      to: "packages/server/src/config/toolRegistry.ts"
      via: "CSHARP_FV_CMD env var read at module load via process.env"
      pattern: "process\\.env\\.CSHARP_FV_CMD"
    - from: "packages/server/.env"
      to: "scripts/hupyy-csharp-verify.sh"
      via: "CS_FV_DLL env var consumed by the wrapper script"
      pattern: "CS_FV_DLL"
---

<objective>
Make the C# FV command configurable via environment variables and fix the local .env file to point to correct local tool paths.

Purpose: Allows a developer to run C# FV locally by setting CSHARP_FV_CMD, CS_FV_DLL, JAVA_HOME, and CVC5_PATH in .env rather than relying on Docker paths.
Output: Updated toolRegistry.ts with env-based command, updated packages/server/.env with correct local paths.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/server/src/config/toolRegistry.ts
@packages/server/.env
@scripts/hupyy-csharp-verify.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make csharp-verification command env-configurable in toolRegistry</name>
  <files>packages/server/src/config/toolRegistry.ts</files>
  <action>
    In packages/server/src/config/toolRegistry.ts, update the csharp-verification entry to use CSHARP_FV_CMD env var with fallback:

    Change:
    ```typescript
    {
      id: 'csharp-verification',
      command: '/usr/local/bin/hupyy-csharp-verify',
      ...
    }
    ```

    To:
    ```typescript
    {
      id: 'csharp-verification',
      command: process.env.CSHARP_FV_CMD ?? '/usr/local/bin/hupyy-csharp-verify',
      ...
    }
    ```

    This mirrors the pattern already used for java-verification (JAVA_FV_CMD) at line 29. The fallback /usr/local/bin/hupyy-csharp-verify preserves Docker behavior when CSHARP_FV_CMD is not set.

    No other changes to the file. Do NOT change the available, defaultArgs, or maxExecutionTimeMs fields.
  </action>
  <verify>
    Run: grep -n "CSHARP_FV_CMD" packages/server/src/config/toolRegistry.ts
    Expected: one line showing process.env.CSHARP_FV_CMD ?? '/usr/local/bin/hupyy-csharp-verify'

    Run: cd /Users/alexanderfedin/Projects/hapyy/languages-web-portal && npm run typecheck
    Expected: no TypeScript errors
  </verify>
  <done>toolRegistry.ts reads CSHARP_FV_CMD from env with fallback to /usr/local/bin/hupyy-csharp-verify; typecheck passes</done>
</task>

<task type="auto">
  <name>Task 2: Update packages/server/.env with correct local tool paths</name>
  <files>packages/server/.env</files>
  <action>
    Rewrite packages/server/.env to add/fix the following variables. Preserve all existing variables (NODE_ENV, PORT, LOG_LEVEL, VERSION, JAVA_FV_JAR, JAVA_FV_CMD) and update/add:

    1. Fix JAVA_HOME: change jdk-22.jdk to jdk-21.jdk
       - Old: JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-22.jdk/Contents/Home
       - New: JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home

    2. Add CSHARP_FV_CMD pointing to the local wrapper script:
       CSHARP_FV_CMD=/Users/alexanderfedin/Projects/hapyy/languages-web-portal/scripts/hupyy-csharp-verify.sh

    3. Add CS_FV_DLL pointing to the local publish output:
       CS_FV_DLL=/Users/alexanderfedin/Projects/hapyy/cs-fv/src/CsFv.Cli/bin/Release/net8.0/publish/cs-fv.dll

    4. Add CVC5_PATH for the wrapper script (cvc5 at ~/bin/cvc5 expands to /Users/alexanderfedin/bin/cvc5):
       CVC5_PATH=/Users/alexanderfedin/bin/cvc5

    Use inline comments (# comment) to document each non-obvious variable. The final .env content should be:

    ```
    NODE_ENV=development
    PORT=3000
    LOG_LEVEL=debug
    VERSION=0.0.0

    # Java FV — local paths
    JAVA_FV_JAR=/Users/alexanderfedin/Projects/hapyy/java-fv/compiler-plugin/cli/target/cli-1.1.0-jar-with-dependencies.jar
    JAVA_FV_CMD=/Users/alexanderfedin/Projects/hapyy/languages-web-portal/scripts/hupyy-java-verify.sh
    JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home

    # C# FV — local paths
    # CSHARP_FV_CMD: overrides toolRegistry default (/usr/local/bin/hupyy-csharp-verify)
    CSHARP_FV_CMD=/Users/alexanderfedin/Projects/hapyy/languages-web-portal/scripts/hupyy-csharp-verify.sh
    # CS_FV_DLL: path to the published cs-fv CLI DLL (overrides wrapper default /usr/local/lib/cs-fv/cs-fv.dll)
    CS_FV_DLL=/Users/alexanderfedin/Projects/hapyy/cs-fv/src/CsFv.Cli/bin/Release/net8.0/publish/cs-fv.dll
    # CVC5_PATH: path to cvc5 binary (used by hupyy-csharp-verify.sh if it reads CVC5_PATH)
    CVC5_PATH=/Users/alexanderfedin/bin/cvc5
    ```
  </action>
  <verify>
    Run: grep -E "JAVA_HOME|CSHARP_FV_CMD|CS_FV_DLL|CVC5_PATH" packages/server/.env
    Expected: 4 lines showing correct paths (jdk-21, local wrapper, local DLL, ~/bin expanded)

    Run: test -f /Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home/bin/java && echo "jdk-21 OK"
    Expected: jdk-21 OK

    Run: test -f /Users/alexanderfedin/Projects/hapyy/cs-fv/src/CsFv.Cli/bin/Release/net8.0/publish/cs-fv.dll && echo "DLL OK"
    Expected: DLL OK

    Run: test -f /Users/alexanderfedin/bin/cvc5 && echo "cvc5 OK"
    Expected: cvc5 OK
  </verify>
  <done>
    packages/server/.env has JAVA_HOME=jdk-21, CSHARP_FV_CMD pointing to local wrapper script,
    CS_FV_DLL pointing to local publish output, CVC5_PATH pointing to ~/bin/cvc5.
    All referenced paths exist on disk.
  </done>
</task>

</tasks>

<verification>
1. grep "CSHARP_FV_CMD" packages/server/src/config/toolRegistry.ts — shows env-based lookup
2. grep "jdk-21" packages/server/.env — shows corrected JAVA_HOME
3. npm run typecheck — passes with no errors
4. The pattern in toolRegistry.ts mirrors the existing java-verification JAVA_FV_CMD pattern
</verification>

<success_criteria>
- CSHARP_FV_CMD env var controls which command toolRegistry uses for csharp-verification; unset = fallback to /usr/local/bin/hupyy-csharp-verify
- packages/server/.env has all four vars: CSHARP_FV_CMD, CS_FV_DLL, JAVA_HOME (jdk-21), CVC5_PATH
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create .planning/phases/24-local-dev-setup-and-csharp-fv-configuration/24-01-SUMMARY.md
</output>
