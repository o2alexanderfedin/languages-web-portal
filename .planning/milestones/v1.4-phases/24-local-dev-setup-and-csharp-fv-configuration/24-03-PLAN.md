---
phase: 24-local-dev-setup-and-csharp-fv-configuration
plan: 03
type: execute
wave: 2
depends_on:
  - "24-01"
  - "24-02"
files_modified: []
autonomous: false
requirements:
  - E2E-01
  - E2E-02

must_haves:
  truths:
    - "C# FV tool shows Available status in the portal UI at http://localhost:5173"
    - "Uploading a C# zip and clicking Execute produces real-time streaming verification output in the browser"
    - "No Docker is required — the local dotnet + cs-fv.dll + cvc5 chain handles execution"
  artifacts:
    - path: "packages/server/.env"
      provides: "runtime config with CSHARP_FV_CMD, CS_FV_DLL, CVC5_PATH, JAVA_HOME"
      contains: "CSHARP_FV_CMD"
    - path: "packages/server/src/config/toolRegistry.ts"
      provides: "env-driven command for csharp-verification"
      contains: "process.env.CSHARP_FV_CMD"
  key_links:
    - from: "browser at http://localhost:5173"
      to: "server GET /api/tools"
      via: "client fetches tool list on load — shows Available when csharp-verification.available=true"
      pattern: "available.*true"
    - from: "browser Execute click"
      to: "server POST /api/execute -> CSHARP_FV_CMD -> cs-fv.dll -> cvc5"
      via: "SSE streaming — output appears in real time"
      pattern: "text/event-stream"
---

<objective>
Human verifies that the complete local development stack works end-to-end: single `npm run dev` starts both processes, C# FV shows Available, and executing a C# zip produces real streaming output.

Purpose: Confirms that plans 01 and 02 wire together correctly in a running browser session.
Output: Verified working local dev environment for C# FV.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-local-dev-setup-and-csharp-fv-configuration/24-01-SUMMARY.md
@.planning/phases/24-local-dev-setup-and-csharp-fv-configuration/24-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-flight checks before human verification</name>
  <files></files>
  <action>
    Run the following checks to confirm the prerequisite artifacts are in place before asking the human to start the server:

    1. Verify CSHARP_FV_CMD is in toolRegistry.ts:
       grep -n "CSHARP_FV_CMD" /Users/alexanderfedin/Projects/hapyy/languages-web-portal/packages/server/src/config/toolRegistry.ts

    2. Verify .env has all four required vars:
       grep -E "CSHARP_FV_CMD|CS_FV_DLL|JAVA_HOME|CVC5_PATH" /Users/alexanderfedin/Projects/hapyy/languages-web-portal/packages/server/.env

    3. Verify the cs-fv.dll exists:
       test -f /Users/alexanderfedin/Projects/hapyy/cs-fv/src/CsFv.Cli/bin/Release/net8.0/publish/cs-fv.dll && echo "cs-fv.dll OK" || echo "MISSING cs-fv.dll"

    4. Verify cvc5 binary exists:
       test -f /Users/alexanderfedin/bin/cvc5 && echo "cvc5 OK" || echo "MISSING cvc5"

    5. Verify concurrently is in root package.json:
       node -e "const p=JSON.parse(require('fs').readFileSync('/Users/alexanderfedin/Projects/hapyy/languages-web-portal/package.json','utf8')); console.log('dev script:', p.scripts.dev.includes('concurrently') ? 'OK' : 'MISSING')"

    6. Verify jdk-21 exists:
       test -d /Library/Java/JavaVirtualMachines/jdk-21.jdk && echo "jdk-21 OK" || echo "MISSING jdk-21"

    If any check reports MISSING, fix the issue before proceeding to the human verification checkpoint.
    If all checks pass, output a summary: "Pre-flight: all checks passed. Ready for human verification."
  </action>
  <verify>
    All six checks output OK/present values (no MISSING outputs).
  </verify>
  <done>Pre-flight confirms toolRegistry has CSHARP_FV_CMD, .env has all four vars, cs-fv.dll exists, cvc5 exists, concurrently configured, jdk-21 present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify end-to-end local C# FV execution</name>
  <files></files>
  <action>Human verifies the full local dev stack end-to-end: npm run dev starts both processes, C# FV shows Available, and executing a C# zip produces real-time streaming output.</action>
  <verify>Human confirms all three acceptance criteria: [server]/[client] labels visible, C# FV shows Available, execution produces streaming output.</verify>
  <done>Human types "approved" confirming end-to-end C# FV works locally without Docker.</done>
  <what-built>
    Plan 01: toolRegistry.ts reads CSHARP_FV_CMD env var; packages/server/.env has CSHARP_FV_CMD, CS_FV_DLL, JAVA_HOME (jdk-21), CVC5_PATH.
    Plan 02: root npm run dev starts server and client concurrently with [server]/[client] labels.
  </what-built>
  <how-to-verify>
    Step 1 — Start the dev stack:
      Open a terminal and run from the project root:
      cd /Users/alexanderfedin/Projects/hapyy/languages-web-portal && npm run dev

      Expected: Terminal shows [server] and [client] labeled output. Server starts on port 3000, client dev server on port 5173. No crash within the first 10 seconds.

    Step 2 — Verify C# FV shows Available:
      Open http://localhost:5173 in a browser.
      Navigate to the tool selector or landing page.
      Find "C# Formal Verification" in the tool list.
      Expected: tool status shows "Available" (not "Unavailable" or grayed out).

    Step 3 — Execute C# FV end-to-end:
      Select "C# Formal Verification" as the active tool.
      Upload a C# zip file (e.g. null-safe-repository.zip from packages/client/public/examples/ or any valid C# project zip with a .csproj).
      Click "Execute".
      Expected: Real-time streaming output appears in the browser output panel showing dotnet/cs-fv verification lines. Output is NOT empty and NOT a timeout error. No Docker process is involved.

    Step 4 — Verify env var fallback behavior (optional but recommended):
      In packages/server/.env, temporarily comment out CSHARP_FV_CMD.
      Restart server (Ctrl+C the dev stack, run npm run dev again).
      The server should still start (using the fallback /usr/local/bin/hupyy-csharp-verify path — it won't be available locally, but the tool registry should load without errors).
      Restore CSHARP_FV_CMD afterward.

    If any step fails, describe the error output seen.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe the failure you observed (step number + error message).</resume-signal>
</task>

</tasks>

<verification>
Human confirms:
1. npm run dev shows [server] and [client] prefixed output
2. http://localhost:5173 shows C# FV as Available
3. Uploading a C# zip and clicking Execute produces real streaming verification output
</verification>

<success_criteria>
- Single npm run dev command brings up both server and client
- C# Formal Verification tool shows Available in the portal
- End-to-end C# FV execution works locally — no Docker required
- Phase 24 success criteria 1-5 from ROADMAP.md all verified
</success_criteria>

<output>
After completion, create .planning/phases/24-local-dev-setup-and-csharp-fv-configuration/24-03-SUMMARY.md
</output>
