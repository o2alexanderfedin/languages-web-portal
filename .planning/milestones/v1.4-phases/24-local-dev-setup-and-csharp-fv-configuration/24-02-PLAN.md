---
phase: 24-local-dev-setup-and-csharp-fv-configuration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - START-01
  - START-02

must_haves:
  truths:
    - "Running npm run dev at project root starts both server (port 3000) and client (port 5173)"
    - "Server output is labeled [server] in the terminal"
    - "Client output is labeled [client] in the terminal"
    - "Both processes run concurrently in a single terminal"
  artifacts:
    - path: "package.json"
      provides: "root dev script using concurrently"
      contains: "concurrently"
  key_links:
    - from: "package.json"
      to: "packages/server (npm run dev -w @repo/server)"
      via: "concurrently with --prefix flag"
      pattern: "concurrently.*server.*client"
    - from: "package.json"
      to: "packages/client (npm run dev -w @repo/client)"
      via: "concurrently with --prefix flag"
      pattern: "concurrently.*client"
---

<objective>
Add a root-level `npm run dev` script that starts both server (port 3000) and client (port 5173) concurrently in a single terminal with labeled prefixes.

Purpose: Developer can clone the repo and start the full local stack with one command instead of opening two terminals.
Output: Updated root package.json with concurrently dev script; concurrently installed as devDependency.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@package.json
@packages/server/package.json
@packages/client/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install concurrently and update root dev script</name>
  <files>package.json</files>
  <action>
    Step 1 — Install concurrently as a root devDependency:
    ```
    cd /Users/alexanderfedin/Projects/hapyy/languages-web-portal && npm install --save-dev concurrently
    ```
    This adds concurrently to the root package.json devDependencies and node_modules.

    Step 2 — Update the "dev" script in root package.json.

    Current dev script (line ~16):
    ```json
    "dev": "npm run dev -w @repo/server",
    ```

    Replace with:
    ```json
    "dev": "concurrently --kill-others-on-fail --prefix-colors \"blue,green\" --prefix \"[{name}]\" --names \"server,client\" \"npm run dev -w @repo/server\" \"npm run dev -w @repo/client\"",
    ```

    Flags explained:
    - `--kill-others-on-fail`: if server crashes, also stop client (prevents zombie processes)
    - `--prefix-colors "blue,green"`: server output blue, client output green
    - `--prefix "[{name}]"`: labels output as [server] and [client]
    - `--names "server,client"`: names match the prefix labels

    Do NOT change any other scripts (build, test, test:e2e, lint, format, typecheck).
    Do NOT change workspaces, engines, or other devDependencies.

    After editing, verify the JSON is valid by running:
    ```
    node -e "JSON.parse(require('fs').readFileSync('package.json', 'utf8'))" && echo "JSON valid"
    ```
  </action>
  <verify>
    Run: node -e "const p = JSON.parse(require('fs').readFileSync('/Users/alexanderfedin/Projects/hapyy/languages-web-portal/package.json', 'utf8')); console.log(p.scripts.dev)"
    Expected: the concurrently command string with --names "server,client"

    Run: grep "concurrently" /Users/alexanderfedin/Projects/hapyy/languages-web-portal/package.json
    Expected: two lines — one in scripts.dev, one in devDependencies

    Run: node -e "require('/Users/alexanderfedin/Projects/hapyy/languages-web-portal/node_modules/concurrently/package.json')" && echo "installed OK"
    Expected: installed OK
  </verify>
  <done>
    Root package.json dev script runs both server and client via concurrently with [server] and [client] prefixes.
    concurrently appears in devDependencies. npm install succeeded.
  </done>
</task>

</tasks>

<verification>
1. cat package.json | node -e "process.stdin.resume(); let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>console.log(JSON.parse(d).scripts.dev))" — shows concurrently command
2. ls node_modules/concurrently — directory exists
3. JSON validity: node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))" — no error
</verification>

<success_criteria>
- npm run dev at project root starts both npm run dev -w @repo/server and npm run dev -w @repo/client
- Output is prefixed [server] and [client] in distinct colors
- concurrently is in root devDependencies with a version pinned by npm
</success_criteria>

<output>
After completion, create .planning/phases/24-local-dev-setup-and-csharp-fv-configuration/24-02-SUMMARY.md
</output>
