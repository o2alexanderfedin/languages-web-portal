---
phase: 25-developer-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - packages/server/.env.example
autonomous: false
requirements:
  - DOC-01

must_haves:
  truths:
    - "README.md exists at project root"
    - "Prerequisites section lists Node.js 22, .NET Runtime 8.0, Java JDK 21, CVC5 1.3.2 (static binary), and Z3 — each with version and install hint"
    - "One-time setup section covers: clone repo, npm install, copy .env.example (or manually create packages/server/.env) with the four required env vars"
    - "Daily start section shows `npm run dev` as the single command with expected output ([server] on :3000, [client] on :5173)"
    - "Java FV local limitation is documented so a developer is not confused when it shows Unavailable"
    - "A developer following only the README steps can reach http://localhost:5173 with C# FV showing Available"
  artifacts:
    - path: "README.md"
      provides: "Complete local developer onboarding guide"
      contains: "## Prerequisites"
    - path: "packages/server/.env.example"
      provides: "Template env file developers can copy and edit"
      contains: "CSHARP_FV_CMD"
  key_links:
    - from: "README.md"
      to: "packages/server/.env.example"
      via: "cp packages/server/.env.example packages/server/.env"
      pattern: "\\.env\\.example"
    - from: "README.md"
      to: "npm run dev"
      via: "Start section"
      pattern: "npm run dev"
---

<objective>
Create `README.md` at project root documenting all prerequisites, one-time setup, and daily start command so any developer can onboard without prior knowledge of the project.

Purpose: v1.4 completes local development experience — the working local stack (Phase 24) is only useful if someone else can reproduce it. README bridges that gap.
Output: `README.md` at project root + `packages/server/.env.example` template file.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-local-dev-setup-and-csharp-fv-configuration/24-01-SUMMARY.md
@packages/server/.env
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create packages/server/.env.example with all required env vars</name>
  <files>packages/server/.env.example</files>
  <action>
Create `packages/server/.env.example` as a committed template (unlike `.env` which is gitignored).
Include all vars from `packages/server/.env` but replace the machine-specific paths with placeholders and descriptive comments. Structure:

```
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug
VERSION=0.0.0

# ─── Java FV ──────────────────────────────────────────────────────────────────
# Path to the java-fv CLI jar (not available locally — see README)
# JAVA_FV_JAR=/path/to/java-fv/cli-1.1.0-jar-with-dependencies.jar
# JAVA_FV_CMD=/path/to/languages-web-portal/scripts/hupyy-java-verify.sh
JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home

# ─── C# FV ────────────────────────────────────────────────────────────────────
# Overrides toolRegistry default (/usr/local/bin/hupyy-csharp-verify used in Docker)
CSHARP_FV_CMD=/path/to/languages-web-portal/scripts/hupyy-csharp-verify.sh
# Path to the published cs-fv CLI DLL (dotnet publish output)
CS_FV_DLL=/path/to/cs-fv/src/CsFv.Cli/bin/Release/net8.0/publish/cs-fv.dll
# Path to cvc5 static binary (download from GitHub releases — see README)
CVC5_PATH=/path/to/bin/cvc5
```

The commented-out JAVA_FV_JAR/JAVA_FV_CMD lines signal they exist but are not required (Java FV not available locally).
  </action>
  <verify>
`cat packages/server/.env.example` — shows all four active env vars (JAVA_HOME, CSHARP_FV_CMD, CS_FV_DLL, CVC5_PATH) with /path/to/... placeholder values and descriptive comments.
`git status packages/server/.env.example` — file is tracked (not gitignored).
  </verify>
  <done>
packages/server/.env.example exists with all four required env vars as placeholders and comment blocks explaining each. File is committed-eligible (not gitignored).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write README.md at project root</name>
  <files>README.md</files>
  <action>
Create `README.md` at the project root. Target audience: a developer who just cloned the repo on macOS and has never seen the project. Write in plain, direct prose — no marketing language.

Structure (exact sections):

```markdown
# Hupyy Languages Web Portal

Try Hupyy formal verification and transpiler tools in the browser — upload code, run it, see results.

## Prerequisites

Install these before anything else.

| Tool | Version | Install |
|------|---------|---------|
| Node.js | 22+ | [nodejs.org](https://nodejs.org) or `brew install node` |
| .NET Runtime | 8.0 | [dotnet.microsoft.com](https://dotnet.microsoft.com/download/dotnet/8.0) |
| Java JDK | 21 | `brew install --cask temurin@21` |
| Z3 | any | `brew install z3` |
| CVC5 | 1.3.2 | See below |

### Installing CVC5

CVC5 1.3.2 is not in Homebrew — download the macOS static binary from GitHub releases:

1. Go to https://github.com/cvc5/cvc5/releases/tag/cvc5-1.3.2
2. Download `cvc5-macOS-arm64-static.zip` (Apple Silicon) or `cvc5-macOS-x86_64-static.zip` (Intel)
3. Unzip and place the `cvc5` binary in `~/bin/` (or any directory in your `$PATH`)
4. Make it executable: `chmod +x ~/bin/cvc5`
5. Verify: `cvc5 --version` should print `cvc5 version 1.3.2`

### Building cs-fv (C# Formal Verification CLI)

The C# FV tool requires the `cs-fv` CLI DLL to be published locally:

1. Clone the `cs-fv` repository (ask the team for access)
2. From the `cs-fv` root: `dotnet publish src/CsFv.Cli -c Release -r osx-arm64`
3. Note the output path: `src/CsFv.Cli/bin/Release/net8.0/publish/cs-fv.dll`

## One-Time Setup

### 1. Clone and install dependencies

```bash
git clone <repo-url>
cd languages-web-portal
npm install
```

### 2. Configure environment variables

Copy the env template and fill in your local paths:

```bash
cp packages/server/.env.example packages/server/.env
```

Edit `packages/server/.env` — replace every `/path/to/...` placeholder with absolute paths on your machine:

| Variable | What it points to |
|----------|-------------------|
| `JAVA_HOME` | JDK 21 home directory (e.g. `/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home`) |
| `CSHARP_FV_CMD` | Wrapper script: `<repo>/scripts/hupyy-csharp-verify.sh` |
| `CS_FV_DLL` | Published cs-fv DLL path (from build step above) |
| `CVC5_PATH` | cvc5 binary (e.g. `~/bin/cvc5` or `/usr/local/bin/cvc5`) |

Make the wrapper script executable (one-time):

```bash
chmod +x scripts/hupyy-csharp-verify.sh
```

## Running Locally

```bash
npm run dev
```

This starts both services in one terminal:

```
[server] Listening on http://localhost:3000
[client] Local: http://localhost:5173
```

Open **http://localhost:5173** in your browser.

## Using the Portal

- **C# Formal Verification** — should show **Available**. Upload a `.zip` containing a C# project (with `.csproj` and `.cs` files), click Execute, and see real-time verification output stream in the browser.
- **Java Formal Verification** — will show **Unavailable** locally. The java-fv CLI jar is not built from source in this repo. This is expected.
- Other tools — available via Docker; see Docker section below (not required for C# FV).

## Project Structure

```
languages-web-portal/
├── packages/
│   ├── server/          # Express API (port 3000)
│   │   └── .env         # Machine-local env vars (gitignored — copy from .env.example)
│   └── client/          # React + Vite frontend (port 5173)
├── scripts/
│   └── hupyy-csharp-verify.sh   # C# FV wrapper script
├── e2e/                 # Playwright end-to-end tests
└── package.json         # Root workspace — `npm run dev` starts everything
```

## Running Tests

```bash
# Unit tests
npm test

# E2E tests (requires running portal)
npm run test:e2e

# E2E against Docker stack
npm run test:e2e:docker:up
```
```

Write this content verbatim into `README.md`. Adjust wording only if a factual error is spotted (e.g., path or command wrong) — do not change structure.
  </action>
  <verify>
`test -f README.md` — file exists at project root.
`grep -c "Prerequisites" README.md` — prints 1 or more (section present).
`grep "CSHARP_FV_CMD" README.md` — shows the env var table entry.
`grep "npm run dev" README.md` — shows the start command.
`grep "cvc5-1.3.2" README.md` — shows CVC5 install instructions.
`grep "\.env\.example" README.md` — shows cp command referencing the template.
  </verify>
  <done>
README.md exists at project root. Contains: Prerequisites table (Node 22, .NET 8, Java 21, Z3, CVC5 1.3.2), CVC5 install steps (GitHub releases static binary), cs-fv build steps, one-time setup (clone + npm install + cp .env.example), env var table with all four vars, `npm run dev` start command, expected output URLs, Java FV "Unavailable locally — expected" note.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify README completeness and accuracy</name>
  <what-built>
README.md at project root documenting full local setup. packages/server/.env.example as committed env template.
  </what-built>
  <how-to-verify>
1. Open README.md in your editor or run `cat README.md`
2. Verify Prerequisites table includes: Node.js 22+, .NET Runtime 8.0, Java JDK 21, Z3, CVC5 1.3.2
3. Verify CVC5 install section links to cvc5-1.3.2 GitHub release and gives download/chmod steps
4. Verify one-time setup shows `cp packages/server/.env.example packages/server/.env`
5. Verify env var table lists JAVA_HOME, CSHARP_FV_CMD, CS_FV_DLL, CVC5_PATH with descriptions
6. Verify `npm run dev` is shown as the start command with expected port output
7. Verify Java FV "Unavailable locally — expected" note is present
8. Open packages/server/.env.example and verify it contains placeholder paths for all four vars
  </how-to-verify>
  <resume-signal>Type "approved" if the README accurately covers all setup steps, or describe what is missing or incorrect</resume-signal>
</task>

</tasks>

<verification>
After Task 2 completes and before the checkpoint:
- `test -f README.md` — file exists
- `test -f packages/server/.env.example` — template exists
- `grep "Prerequisites" README.md` — section present
- `grep "npm run dev" README.md` — start command present
- `grep "CSHARP_FV_CMD" README.md` — env var documented
- `grep "\.env\.example" README.md` — template reference present
- `grep "cvc5" README.md | wc -l` — multiple mentions (install + path)
- `git check-ignore packages/server/.env.example` — empty output (file is NOT gitignored, meaning it CAN be committed)
</verification>

<success_criteria>
1. README.md exists at project root covering all three success criteria from ROADMAP Phase 25:
   - All prerequisites listed (Node 22, .NET 8, Java 21, CVC5 1.3.2, Z3) with install hints
   - Steps documented: clone, npm install, cp .env.example, edit env vars, npm run dev → browser at :5173
   - Both one-time setup and daily start command documented
2. packages/server/.env.example committed as a template (not gitignored) with placeholder values for all four env vars
3. Human approves README at checkpoint
4. DOC-01 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/25-developer-readme/25-01-SUMMARY.md`
</output>
