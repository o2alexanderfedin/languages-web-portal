---
phase: 21-wrapper-script-tool-registry-activation
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01
  - 21-02
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - CSFV-04
gap_closure: true

must_haves:
  truths:
    - "REQUIREMENTS.md CSFV-04 status is pending (not marked complete) until Phase 22 creates the example .csproj files"
    - "REQUIREMENTS.md Traceability table correctly shows CSFV-04 as pending at Phase 22, not complete at Phase 21"
    - "REQUIREMENTS.md accurately reflects the split: Phase 21 delivered wrapper exit-code passthrough; Phase 22 must deliver TreatWarningsAsErrors=true in example .csproj files"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate tracking — CSFV-04 marked pending with note that Phase 22 must deliver the .csproj artifact"
      contains: "CSFV-04"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 22 planning context"
      via: "CSFV-04 pending status signals Phase 22 must include TreatWarningsAsErrors=true in example .csproj files"
      pattern: "CSFV-04.*Phase 22.*Pending"
---

<objective>
Fix REQUIREMENTS.md tracking accuracy: CSFV-04 is prematurely marked `[x] Complete` at Phase 21, but the required artifact (example `.csproj` files with `<TreatWarningsAsErrors>true</TreatWarningsAsErrors>`) does not exist — it is a Phase 22 deliverable.

Purpose: VERIFICATION.md (score 8/9) identified that CSFV-04 was partially satisfied — the wrapper's exit-code passthrough is correct, but the `.csproj` artifact is absent. Marking it complete at Phase 21 would cause Phase 22 planning to miss this requirement. The fix ensures Phase 22 knows it must deliver the `.csproj` files with TreatWarningsAsErrors to fully close CSFV-04.

Output:
- `.planning/REQUIREMENTS.md` — CSFV-04 changed from `[x]` to `[ ]` with an explanatory note; Traceability table updated to show Phase 22 as responsible with Pending status
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/21-wrapper-script-tool-registry-activation/21-VERIFICATION.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct CSFV-04 tracking status in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Make two targeted edits to `.planning/REQUIREMENTS.md` to accurately reflect CSFV-04's split delivery across phases.

**Edit 1: C# FV Integration checklist — revert CSFV-04 to pending with note**

Find the line:
```
- [x] **CSFV-04**: Wrapper script handles Roslyn `Warning`-severity exit code 0 via `<TreatWarningsAsErrors>true</TreatWarningsAsErrors>` in example `.csproj` files
```

Replace with:
```
- [ ] **CSFV-04**: Wrapper script handles Roslyn `Warning`-severity exit code 0 via `<TreatWarningsAsErrors>true</TreatWarningsAsErrors>` in example `.csproj` files _(Phase 21 delivered wrapper exit-code passthrough; Phase 22 must deliver example `.csproj` files with this property)_
```

**Edit 2: Traceability table — move CSFV-04 to Phase 22, Pending**

Find the traceability table row:
```
| CSFV-04 | Phase 21 | Complete |
```

Replace with:
```
| CSFV-04 | Phase 22 | Pending |
```

(Phase 21 completed the wrapper's exit-code passthrough contribution; the `.csproj` artifact is Phase 22's responsibility.)

**IMPORTANT constraints:**
- Do NOT modify CSFV-01, CSFV-02, or CSFV-03 — those remain `[x] Complete` at Phase 21
- Do NOT modify any other requirement entries
- Do NOT change the `Last updated` metadata line — leave as-is
- Only these two edits: checklist line and traceability table row for CSFV-04
  </action>
  <verify>
```bash
# 1. CSFV-04 checklist shows [ ] (not [x])
grep "CSFV-04" .planning/REQUIREMENTS.md | head -5

# 2. Traceability table shows Phase 22 / Pending for CSFV-04
grep -A1 "CSFV-04" .planning/REQUIREMENTS.md | grep "Phase 22.*Pending"

# 3. CSFV-01/02/03 are still marked complete
grep "CSFV-0[123]" .planning/REQUIREMENTS.md | grep "\[x\]"

# 4. No [x] remains for CSFV-04 in the checklist
grep "CSFV-04" .planning/REQUIREMENTS.md | grep "\[x\]" && echo "FAIL: CSFV-04 still marked complete" || echo "OK: CSFV-04 correctly pending"
```
  </verify>
  <done>
- `.planning/REQUIREMENTS.md` CSFV-04 checklist entry is `[ ]` (pending) with a note explaining the split delivery
- Traceability table shows `CSFV-04 | Phase 22 | Pending`
- CSFV-01, CSFV-02, CSFV-03 remain `[x] Complete` at Phase 21 — unchanged
- No other lines modified
  </done>
</task>

</tasks>

<verification>
```bash
# Full check of CSFV lines in REQUIREMENTS.md
grep "CSFV" .planning/REQUIREMENTS.md

# Confirm checklist: CSFV-01/02/03 complete, CSFV-04 pending
grep -E "CSFV-0[1-4]" .planning/REQUIREMENTS.md | grep "^\-"

# Confirm traceability table
grep "CSFV" .planning/REQUIREMENTS.md | grep "|"
```
</verification>

<success_criteria>
- CSFV-04 checklist entry shows `[ ]` with explanatory note about Phase 21 wrapper vs Phase 22 .csproj artifact
- Traceability table row for CSFV-04 points to Phase 22 with status Pending
- CSFV-01, CSFV-02, CSFV-03 remain complete at Phase 21 — no collateral changes
- REQUIREMENTS.md accurately reflects what was and was not delivered in Phase 21
</success_criteria>

<output>
After completion, create `.planning/phases/21-wrapper-script-tool-registry-activation/21-03-SUMMARY.md`
</output>
