---
phase: 17-edge-cases-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/tests/tool-switching.spec.ts
  - e2e/tests/browser-navigation.spec.ts
autonomous: true
requirements:
  - EDGE-03
  - EDGE-04

must_haves:
  truths:
    - "User can select Z3 tool, execute it, then switch to CVC5 and execute again"
    - "Output panel clears (or is replaced) after tool switch before new execution"
    - "CVC5 execution produces console output after switching from Z3"
    - "Navigating Landing → /demo then pressing browser Back returns to Landing"
    - "Pressing browser Forward returns to /demo after pressing Back"
    - "After Back → Forward cycle, the /demo page state is preserved (tool picker visible)"
    - "No JS exceptions during back/forward navigation"
  artifacts:
    - path: "e2e/tests/tool-switching.spec.ts"
      provides: "EDGE-03 Docker-serial tool switching flow tests"
      min_lines: 60
    - path: "e2e/tests/browser-navigation.spec.ts"
      provides: "EDGE-04 back/forward navigation state preservation tests"
      min_lines: 50
  key_links:
    - from: "e2e/tests/tool-switching.spec.ts"
      to: "ExecutionPage.selectTool() + execute()"
      via: "exec.selectTool('z3') then exec.selectTool('cvc5')"
      pattern: "selectTool"
    - from: "e2e/tests/browser-navigation.spec.ts"
      to: "page.goBack() / page.goForward()"
      via: "page.goBack({ waitUntil: 'networkidle' })"
      pattern: "goBack|goForward"
---

<objective>
Create E2E tests for tool switching (EDGE-03, Docker required) and browser back/forward navigation (EDGE-04, route interception).

Purpose: Verify two behavioral edge cases requiring execution state — tool switching correctness and browser history navigation.
Output: e2e/tests/tool-switching.spec.ts (Docker serial) + e2e/tests/browser-navigation.spec.ts (route interception, no Docker).
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@e2e/pages/ExecutionPage.ts
@e2e/pages/DemoPage.ts
@e2e/tests/execution-flow.spec.ts
@e2e/tests/output-download.spec.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tool switching E2E test suite (EDGE-03, Docker)</name>
  <files>e2e/tests/tool-switching.spec.ts</files>
  <action>
    Create e2e/tests/tool-switching.spec.ts following the Docker-serial pattern from execution-flow.spec.ts.

    Import: { test, expect } from '@playwright/test', ExecutionPage from '../pages/ExecutionPage'.

    **describe block:** 'Tool Switching Flow (EDGE-03)'
    - test.setTimeout(180_000) — Docker execution is slow
    - test.describe.configure({ mode: 'serial' }) — avoid Docker contention
    - test.skip(({ isMobile }) => isMobile, 'Tool switching tests run on desktop only')

    **Tool IDs available in the app:** 'java-verification' (active), 'cpp-to-c-transpiler' (coming soon), 'cpp-to-rust-transpiler' (coming soon). Only java-verification is fully active in Docker. Check what tools are actually executable: java-verification uses the docker java wrapper.

    Since only java-verification is Docker-enabled, the tool switching test should cover:
    1. Load example with java-verification (first tool)
    2. Switch to a second tool selection (e.g., cpp-to-c-transpiler card click)
    3. Verify output/console state clears
    4. Switch back to java-verification
    5. Execute and verify new execution produces output

    This is more realistic than Z3→CVC5 (which may not exist in this codebase). Use tool switching between java-verification selections, or if multiple active tools exist, use those. The plan says 'Z3 → CVC5 or CVC5 → Z3 based on what's available in Docker'. Inspect what tools are actually available by checking ToolPicker component — if only java-verification is active, the test should cover:

    **Test 1: 'tool switching clears previous execution state'** —
    Navigate to '/demo?tool=java-verification'. Load 'bank-account-records' example. Execute and wait for completion. Assert consoleOutput has text. Assert outputPanel is visible.
    Click tool option for a different tool (use exec.selectTool('cpp-to-c-transpiler')). The console output and/or output panel should clear or the execute button should reset. Assert executeButton state reflects the new tool state — the button label should change to match new tool OR be disabled (no upload for new tool). Assert consoleOutput no longer shows previous execution text OR outputPanel becomes hidden.

    **Test 2: 'switching back to java-verification and executing produces output'** —
    Navigate to '/demo?tool=java-verification'. Load 'bank-account-records'. Execute, wait for completion. Then click exec.selectTool('java-verification') again (re-select same tool — simulates switch cycle). Load example again. Execute again. Verify COMPLETED status appears.

    **Test 3: 'tool switch before execution does not carry over uploaded file context'** —
    Navigate to '/demo'. Click tool option 'java-verification'. Without uploading or loading an example, click 'cpp-to-c-transpiler'. Assert executeButton is disabled (no file selected for new tool). Assert toolPicker is visible showing the new selection.

    Use existing ExecutionPage locators: exec.executeButton, exec.consoleOutput, exec.outputPanel, exec.toolPicker.
    For tool card clicks: exec.page.getByTestId('tool-option-{toolId}').click() — same as exec.selectTool().

    TypeScript: all methods already typed in ExecutionPage.ts. No new types needed.
  </action>
  <verify>
    npx tsc --noEmit -p /Users/alexanderfedin/Projects/hapyy/languages-web-portal/e2e/tsconfig.json
    wc -l /Users/alexanderfedin/Projects/hapyy/languages-web-portal/e2e/tests/tool-switching.spec.ts
    grep -c "^  test(" /Users/alexanderfedin/Projects/hapyy/languages-web-portal/e2e/tests/tool-switching.spec.ts
  </verify>
  <done>
    TypeScript compiles with zero errors. File is at least 60 lines. Contains 3 tests. describe block has isMobile skip, test.setTimeout(180_000), and serial mode. No imports outside @playwright/test and ExecutionPage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create browser navigation E2E test suite (EDGE-04, route interception)</name>
  <files>e2e/tests/browser-navigation.spec.ts</files>
  <action>
    Create e2e/tests/browser-navigation.spec.ts using route interception pattern (no Docker) from output-download.spec.ts.

    Import: { test, expect } from '@playwright/test', DemoPage from '../pages/DemoPage'.

    **describe block:** 'Browser Back/Forward Navigation (EDGE-04)'
    - test.skip(({ isMobile }) => isMobile, 'Browser navigation tests run on desktop only') — Chromium only per user decision
    - NO test.setTimeout override (no Docker)
    - NO serial mode (tests are independent)

    **Route interception for simulated execution state:**
    Use page.route('**/execute**', route => route.fulfill({ status: 200, contentType: 'text/event-stream', body: 'data: {"type":"output","content":"Verification complete"}\n\ndata: {"type":"exit","exitCode":0}\n\n' }))
    — same approach as execution-errors.spec.ts / output-download.spec.ts for mocked execution without Docker.

    **pageErrors capture:** const pageErrors: string[] = []; page.on('pageerror', err => pageErrors.push(err.message)); — assert length 0 at end of each navigation test.

    **Tests to write:**

    1. 'navigating Landing → /demo then Back returns to Landing' —
    Go to '/'. Wait for landing page to load (assert page.getByTestId('landing-page') is visible). Click a Try Now link to navigate to '/demo'. Assert URL contains '/demo'. Call page.goBack({ waitUntil: 'networkidle' }). Assert URL is '/'. Assert page.getByTestId('landing-page') is visible again. Assert zero JS errors.

    2. 'pressing Forward after Back returns to /demo with tool picker visible' —
    Go to '/'. Navigate to '/demo' via page.goto('/demo'). Assert toolPicker visible. Call page.goBack(). Assert URL '/'. Call page.goForward({ waitUntil: 'networkidle' }). Assert URL contains '/demo'. Assert demo.toolPicker is visible. Assert zero JS errors.

    3. 'direct navigation to /demo?tool=java-verification then Back returns to Landing' —
    Go to '/'. Then page.goto('/demo?tool=java-verification'). Assert tool card selected (getTestId('tool-option-java-verification') has border-primary). Call page.goBack(). Assert landing-page visible. Assert zero JS errors.

    4. 'back/forward preserves tool pre-selection from URL params' —
    Go to '/demo?tool=java-verification'. Assert tool option selected. Go back (page.goBack()). Go forward (page.goForward()). Assert URL still contains 'tool=java-verification'. Assert tool-option-java-verification still has border-primary class. This verifies browser history state is correctly restored.

    Use DemoPage for locators: demo.toolPicker, demo.executeButton, demo.getToolOption('java-verification').
    Use page.url() for URL assertions.
    Use page.waitForURL() where needed: await page.waitForURL('**/demo**', { timeout: 10_000 }).

    TypeScript: import type not needed beyond the existing DemoPage import. Keep strict.
    Compile: npx tsc --noEmit -p e2e/tsconfig.json
  </action>
  <verify>
    npx tsc --noEmit -p /Users/alexanderfedin/Projects/hapyy/languages-web-portal/e2e/tsconfig.json
    wc -l /Users/alexanderfedin/Projects/hapyy/languages-web-portal/e2e/tests/browser-navigation.spec.ts
    grep -c "^  test(" /Users/alexanderfedin/Projects/hapyy/languages-web-portal/e2e/tests/browser-navigation.spec.ts
  </verify>
  <done>
    TypeScript compiles with zero errors. File is at least 50 lines. Contains 4 tests. describe block has isMobile skip. No Docker dependency (no test.setTimeout, no serial mode, no E2E_BASE_URL references). pageErrors captured in all navigation tests.
  </done>
</task>

</tasks>

<verification>
1. npx tsc --noEmit -p e2e/tsconfig.json — zero errors across all e2e files (including both new files)
2. e2e/tests/tool-switching.spec.ts: exists, >= 60 lines, 3 tests, serial + 180s timeout, isMobile skip
3. e2e/tests/browser-navigation.spec.ts: exists, >= 50 lines, 4 tests, isMobile skip, no Docker
4. No pageErrors in navigation tests (captured pattern present)
</verification>

<success_criteria>
- e2e/tests/tool-switching.spec.ts exists: 3 tests, Docker-serial, covers tool selection state changes (EDGE-03)
- e2e/tests/browser-navigation.spec.ts exists: 4 tests, route-interception, covers Landing↔/demo back/forward (EDGE-04)
- TypeScript compiles clean across all 20 e2e test files
- Both files use isMobile skip guard (Chromium desktop only per user decision)
- Phase 17 complete: all 4 requirements covered (EDGE-01 in 17-01, EDGE-02 in 17-01, EDGE-03 in 17-02, EDGE-04 in 17-02)
</success_criteria>

<output>
After completion, create `.planning/phases/17-edge-cases-polish/17-02-SUMMARY.md`
</output>
