---
phase: 17-edge-cases-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/tests/theme-and-404.spec.ts
autonomous: true
requirements:
  - EDGE-01
  - EDGE-02

must_haves:
  truths:
    - "Theme toggle cycles Light → Dark → System on the Landing page"
    - "Selected theme class ('light' or 'dark') is present on html element after toggle"
    - "localStorage 'hupyy-theme' value matches the selected theme after toggle"
    - "Theme persists after navigating Landing → /demo and back"
    - "Theme persists after page.reload() (hard refresh)"
    - "System mode applies html class matching emulated prefers-color-scheme"
    - "Navigating to /nonexistent renders '404 - Page Not Found' text"
    - "Navigating to /totally-invalid-path renders '404 - Page Not Found' text"
    - "404 page produces zero JS exceptions"
  artifacts:
    - path: "e2e/tests/theme-and-404.spec.ts"
      provides: "EDGE-01 theme toggle persistence tests + EDGE-02 404 routing tests"
      min_lines: 80
  key_links:
    - from: "e2e/tests/theme-and-404.spec.ts"
      to: "ThemeProvider localStorage key 'hupyy-theme'"
      via: "page.evaluate(() => localStorage.getItem('hupyy-theme'))"
      pattern: "localStorage\\.getItem\\('hupyy-theme'\\)"
    - from: "e2e/tests/theme-and-404.spec.ts"
      to: "document.documentElement.classList"
      via: "page.evaluate(() => document.documentElement.classList.contains('dark'))"
      pattern: "documentElement\\.classList"
---

<objective>
Create E2E tests for theme toggle persistence (EDGE-01) and 404 routing (EDGE-02).

Purpose: Verify the two stateless edge cases that don't require Docker — theme persistence across navigation and hard reload, and correct 404 rendering for unknown routes.
Output: e2e/tests/theme-and-404.spec.ts — Chromium desktop, no Docker dependency.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/client/src/components/ThemeProvider.tsx
@packages/client/src/pages/Landing.tsx
@packages/client/src/App.tsx
@e2e/tests/shareable-links-cross-browser.spec.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create theme toggle and 404 E2E test suite (EDGE-01 + EDGE-02)</name>
  <files>e2e/tests/theme-and-404.spec.ts</files>
  <action>
    Create e2e/tests/theme-and-404.spec.ts with two describe blocks:

    **File header pattern:** Same as shareable-links-cross-browser.spec.ts — import { test, expect } from '@playwright/test', no POM imports needed (direct page locators only).

    **EDGE-01: Theme Toggle Persistence** describe block.
    Apply test.skip(({ isMobile }) => isMobile, 'Theme tests run on desktop only') — Chromium desktop.
    No test.describe.configure serial mode (tests are independent, no Docker).

    Theme toggle button locator: page.getByRole('button', { name: /Theme:/i })
    localStorage key: 'hupyy-theme'
    CSS class assertion helper: page.evaluate(() => document.documentElement.classList.contains('dark'))

    Tests to write:

    1. 'theme toggle button cycles light → dark → system' — goto '/'. Click toggle once (system→light), assert button text contains 'Light' and html has class 'light'. Click again (light→dark), assert button text contains 'Dark' and html has class 'dark'. Click again (dark→system), assert button text contains 'System'.

    2. 'selecting light theme persists localStorage key' — goto '/'. Find starting theme, click toggle button until label is 'Light' (up to 3 clicks). Assert localStorage.getItem('hupyy-theme') === 'light'.

    3. 'selecting dark theme applies dark class to html element' — goto '/'. Click toggle until label shows 'Dark'. Assert document.documentElement.classList.contains('dark') === true. Assert document.documentElement.classList.contains('light') === false.

    4. 'system mode with dark preference applies dark class to html' — goto '/'. Click toggle until label shows 'System'. Use page.emulateMedia({ colorScheme: 'dark' }). Trigger re-evaluation by navigating to '/' again. Assert html has class 'dark' (system resolved to dark per emulated preference).

    5. 'dark theme persists across Landing → /demo navigation' — goto '/'. Click toggle until label shows 'Dark'. Navigate to '/demo'. Assert document.documentElement.classList.contains('dark') === true (ThemeProvider survives route change since it wraps BrowserRouter).

    6. 'dark theme persists after hard reload (page.reload())' — goto '/'. Click toggle until label shows 'Dark'. Assert localStorage.getItem('hupyy-theme') === 'dark'. Call page.reload(). Assert document.documentElement.classList.contains('dark') === true and localStorage still 'dark'.

    Helper pattern for clicking to a target theme label:
    ```typescript
    async function clickToTheme(page: Page, targetLabel: string) {
      for (let i = 0; i < 3; i++) {
        const btn = page.getByRole('button', { name: /Theme:/i });
        const text = await btn.textContent();
        if (text?.includes(targetLabel)) break;
        await btn.click();
      }
    }
    ```

    **EDGE-02: 404 Routing** describe block.
    Apply test.skip(({ isMobile }) => isMobile, '404 tests run on desktop only').

    Tests to write:

    7. '/nonexistent route renders 404 page content' — navigate to '/nonexistent'. Assert page.getByText('404 - Page Not Found') is visible. Assert zero JS errors (use pageErrors capture pattern from shareable-links-cross-browser.spec.ts).

    8. '/totally-invalid-path also renders 404' — navigate to '/totally-invalid-path'. Assert page.getByText('404 - Page Not Found') is visible.

    9. '404 page HTTP response check' — use page.request.get('/nonexistent') to get the raw response. Note: React SPA served via Vite dev server may return 200 with HTML even for unknown routes (SPA fallback). Assert the rendered page content includes '404 - Page Not Found' text. Do NOT assert HTTP status 404 — Vite dev server and Docker Nginx may differ. Comment this distinction clearly.

    TypeScript: import type { Page } from '@playwright/test' for the helper function parameter type.
    Compile check: npx tsc --noEmit -p e2e/tsconfig.json
  </action>
  <verify>
    npx tsc --noEmit -p /Users/alexanderfedin/Projects/hapyy/languages-web-portal/e2e/tsconfig.json
    wc -l /Users/alexanderfedin/Projects/hapyy/languages-web-portal/e2e/tests/theme-and-404.spec.ts
    grep -c "^  test(" /Users/alexanderfedin/Projects/hapyy/languages-web-portal/e2e/tests/theme-and-404.spec.ts
  </verify>
  <done>
    TypeScript compiles with zero errors. File is at least 80 lines. Contains 9 tests (6 EDGE-01 + 3 EDGE-02). Both describe blocks have isMobile skip guards. No Docker dependency (no test.setTimeout or serial mode).
  </done>
</task>

</tasks>

<verification>
1. npx tsc --noEmit -p e2e/tsconfig.json — zero errors across all e2e files
2. File exists: e2e/tests/theme-and-404.spec.ts
3. Line count >= 80
4. 9 tests present (6 EDGE-01 theme tests + 3 EDGE-02 404 tests)
5. No Docker references (no test.setTimeout, no serial mode, no E2E_BASE_URL)
6. Both describe blocks have test.skip(isMobile) guard
</verification>

<success_criteria>
- e2e/tests/theme-and-404.spec.ts exists with 9 tests
- TypeScript compiles clean
- EDGE-01 covered: 6 tests covering light/dark/system toggle, localStorage persistence, CSS class application, navigation persistence, and hard-reload persistence
- EDGE-02 covered: 3 tests covering /nonexistent and /totally-invalid-path routes rendering '404 - Page Not Found', zero JS exceptions
- Chromium-desktop only (isMobile skip in both describe blocks)
</success_criteria>

<output>
After completion, create `.planning/phases/17-edge-cases-polish/17-01-SUMMARY.md`
</output>
