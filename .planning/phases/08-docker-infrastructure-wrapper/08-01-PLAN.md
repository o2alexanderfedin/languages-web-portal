---
phase: 08-docker-infrastructure-wrapper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
autonomous: true

must_haves:
  truths:
    - "Docker image successfully builds with JDK 25 installed"
    - "Java FV CLI jar exists at /usr/local/lib/java-fv-cli.jar in production image"
    - "Maven build compiles Java FV from source during Docker build"
    - "Z3 solver is bundled via z3-turnkey dependency (no separate installation)"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage build with JDK 25 and Java FV CLI jar"
      min_lines: 80
      contains: "eclipse-temurin:25-jdk"
  key_links:
    - from: "Dockerfile builder stage"
      to: "/Users/alexanderfedin/Projects/hapyy/java-fv"
      via: "COPY java-fv source and mvn package"
      pattern: "COPY.*java-fv.*mvn.*package"
    - from: "Dockerfile production stage"
      to: "/usr/local/lib/java-fv-cli.jar"
      via: "COPY built jar from builder stage"
      pattern: "COPY --from=.*java-fv.*\\.jar"
---

<objective>
Update Dockerfile to build production image with JDK 25, Maven, and Java FV CLI jar compiled from source during image build.

Purpose: Enable Java verification tool execution in production Docker environment by providing required JDK runtime and pre-compiled CLI jar at known path.

Output: Modified Dockerfile that produces production image with JDK 25 and `/usr/local/lib/java-fv-cli.jar` ready for wrapper script invocation.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Current Dockerfile uses node:22-alpine multi-stage build (builder + production). User chose "Full JDK in production" approach per STATE.md notes.

Java FV source location: /Users/alexanderfedin/Projects/hapyy/java-fv/compiler-plugin/
CLI module: /Users/alexanderfedin/Projects/hapyy/java-fv/compiler-plugin/cli/
Built jar output: cli/target/cli-1.1.0-jar-with-dependencies.jar

JDK requirement: JDK 25 (Microsoft OpenJDK or Eclipse Temurin)
Maven requirement: 3.9+
Z3 solver: Bundled via z3-turnkey 4.14.1 (already in Maven dependencies, no separate install)

@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Update Dockerfile to 3-stage build with JDK 25 and Java FV compilation</name>
  <files>Dockerfile</files>
  <action>
Modify existing 2-stage Dockerfile to 3-stage build:

**Stage 1 - node-builder** (unchanged logic, rename for clarity):
- Base: node:22-alpine
- Build TypeScript portal (shared + server + client)
- Output: /app/packages/*/dist

**Stage 2 - java-builder** (NEW):
- Base: eclipse-temurin:25-jdk (Debian-based with JDK 25, ~200MB but necessary)
- Install Maven 3.9+ via apt: `apt-get update && apt-get install -y maven`
- COPY java-fv source: `COPY /Users/alexanderfedin/Projects/hapyy/java-fv /java-fv`
- Build CLI jar: `cd /java-fv/compiler-plugin && mvn clean package -DskipTests`
- Output: /java-fv/compiler-plugin/cli/target/cli-1.1.0-jar-with-dependencies.jar

**Stage 3 - production** (modified):
- Base: eclipse-temurin:25-jre-noble (Ubuntu Noble with JRE only, smaller than JDK)
- Install Node.js 22 runtime: Use NodeSource setup script for Ubuntu Noble
- COPY built TypeScript artifacts from node-builder stage
- COPY Java FV CLI jar from java-builder: `COPY --from=java-builder /java-fv/compiler-plugin/cli/target/cli-1.1.0-jar-with-dependencies.jar /usr/local/lib/java-fv-cli.jar`
- Install production npm dependencies
- Keep existing uploads directory, user setup, healthcheck, CMD

Why JRE in production: JDK only needed for Maven build (stage 2). Production only needs JRE to run `java -jar`.

Why not alpine: Java FV may have native dependencies; Debian/Ubuntu base more compatible.
  </action>
  <verify>
    <command>docker build -t languages-portal-test .</command>
    <expected>Build succeeds, production image contains /usr/local/lib/java-fv-cli.jar</expected>
  </verify>
  <done>
    - Docker build completes successfully without errors
    - Production image based on eclipse-temurin:25-jre-noble
    - File /usr/local/lib/java-fv-cli.jar exists in production image
    - Image includes both Node.js 22 runtime (for portal server) and JRE 25 (for Java FV)
    - Build time increased by ~3-5 minutes (acceptable for demo portal)
  </done>
</task>

<task type="auto">
  <name>Verify Docker image contains working Java runtime and CLI jar</name>
  <files>N/A (verification only)</files>
  <action>
Test built Docker image to confirm:
1. Java runtime available: `docker run --rm languages-portal-test java -version` → shows JDK 25
2. CLI jar exists: `docker run --rm languages-portal-test ls -lh /usr/local/lib/java-fv-cli.jar` → shows jar with size ~20-50MB
3. CLI jar is executable: `docker run --rm languages-portal-test java -jar /usr/local/lib/java-fv-cli.jar --help` → shows Java FV CLI usage

If any check fails: Debug layer-by-layer with `docker build --target java-builder` and inspect intermediate images.
  </action>
  <verify>
    <command>All three docker run verification commands succeed</command>
  </verify>
  <done>
    - `java -version` shows OpenJDK 25 (Eclipse Temurin)
    - `/usr/local/lib/java-fv-cli.jar` file exists and is 20-50MB
    - `java -jar /usr/local/lib/java-fv-cli.jar --help` displays CLI help message without errors
  </done>
</task>

</tasks>

<verification>
**Code verification:**
1. Run `docker build -t languages-portal-test .` → succeeds
2. Check production image base: `docker inspect languages-portal-test | grep -i "temurin:25"` → confirms JRE base
3. Verify Node.js available: `docker run --rm languages-portal-test node --version` → v22.x
4. Verify Java available: `docker run --rm languages-portal-test java -version` → OpenJDK 25
5. Verify CLI jar: `docker run --rm languages-portal-test java -jar /usr/local/lib/java-fv-cli.jar --version` → shows version

**Must-haves check:**
- Truth 1: Docker build completes ✓
- Truth 2: CLI jar at /usr/local/lib/java-fv-cli.jar ✓
- Truth 3: Maven compiled Java FV during build ✓ (stage 2 logs show mvn package)
- Truth 4: Z3 bundled (no separate install steps in Dockerfile) ✓
- Artifact: Dockerfile contains eclipse-temurin:25 references ✓
- Key link 1: Builder stage copies java-fv and runs mvn ✓
- Key link 2: Production copies jar from builder ✓
</verification>

<success_criteria>
**Measurable outcomes:**
1. `docker build` completes in 5-10 minutes (increased from current ~2 min due to Maven build)
2. Production image size: 600-800MB (Node.js base ~150MB + JRE 25 ~200MB + portal ~50MB + Java FV jar ~50MB + dependencies)
3. Docker image tagged and ready for Plan 02 wrapper integration
4. No runtime errors when invoking `java -jar /usr/local/lib/java-fv-cli.jar`

**Requirements satisfied:**
- ✅ DOCK-01: JDK 25 with Java FV CLI jar pre-installed
- ✅ DOCK-03: Docker build compiles java-fv from source
- ✅ DOCK-04: Java FV execution works with proper JDK paths and Z3
</success_criteria>

<output>
After completion, create `.planning/phases/08-docker-infrastructure-wrapper/08-01-SUMMARY.md`
</output>
