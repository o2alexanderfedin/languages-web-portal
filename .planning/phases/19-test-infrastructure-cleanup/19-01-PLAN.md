---
phase: 19-test-infrastructure-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/fixtures/helpers.ts
  - playwright.config.ts
  - e2e/pages/LandingPage.ts
  - e2e/tests/browser-navigation.spec.ts
autonomous: true
requirements:
  - INFRA-03
  - INFRA-04
  - EDGE-04

must_haves:
  truths:
    - "`loadExampleAndExecute()` is gone from helpers.ts — no callers reference it"
    - "Running `npx playwright test` without `E2E_BASE_URL` set aborts with an actionable error message"
    - "All landing page assertions in browser-navigation.spec.ts go through LandingPage POM"
    - "`LandingPage` POM exposes a `landingContainer` locator for `data-testid='landing-page'`"
  artifacts:
    - path: "e2e/fixtures/helpers.ts"
      provides: "Shared test utilities (path constants + general helpers, no DemoPage dependency)"
      contains: "SAMPLE_ZIP_PATH"
    - path: "playwright.config.ts"
      provides: "Playwright config with Docker guard and POM contract comment"
      contains: "E2E_BASE_URL"
    - path: "e2e/pages/LandingPage.ts"
      provides: "LandingPage POM with landingContainer locator"
      contains: "landingContainer"
    - path: "e2e/tests/browser-navigation.spec.ts"
      provides: "EDGE-04 tests using LandingPage POM for all landing interactions"
      contains: "LandingPage"
  key_links:
    - from: "e2e/tests/browser-navigation.spec.ts"
      to: "e2e/pages/LandingPage.ts"
      via: "import LandingPage, use landing.goto() and landing.landingContainer"
      pattern: "LandingPage"
    - from: "playwright.config.ts"
      to: "process.env.E2E_BASE_URL"
      via: "top-level guard that throws before defineConfig runs"
      pattern: "E2E_BASE_URL"
---

<objective>
Remove the orphaned `loadExampleAndExecute()` helper, add a hard-fail Docker guard to playwright.config.ts, and fix the LandingPage POM bypass in browser-navigation.spec.ts.

Purpose: Eliminates dead code (INFRA-04), enforces the Docker-required test invariant (INFRA-03), and ensures EDGE-04 tests are mediated through the LandingPage POM (EDGE-04).
Output: Cleaned helpers.ts, guarded playwright.config.ts, updated LandingPage POM and browser-navigation.spec.ts
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@e2e/fixtures/helpers.ts
@e2e/pages/LandingPage.ts
@e2e/tests/browser-navigation.spec.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove loadExampleAndExecute from helpers.ts</name>
  <files>e2e/fixtures/helpers.ts</files>
  <action>
    Audit all files under e2e/ for imports or calls to `loadExampleAndExecute`. Grep confirms there are zero callers — the function is only defined, never imported.

    Delete lines 77-95 from helpers.ts (the `loadExampleAndExecute` async function and its JSDoc block). The remaining exports — `SAMPLE_ZIP_PATH`, `INVALID_TXT_PATH`, `INVALID_JPG_PATH`, `EMPTY_ZIP_PATH`, `NO_EXTENSION_PATH`, `waitForExecutionComplete`, `createTempFile`, `cleanupTempFile`, `createOversizedFile` — must be kept intact.

    Also remove the `import { DemoPage } from '../pages/DemoPage'` import at line 5, since it is only used by the now-deleted function and would become an unused import (TypeScript strict mode will flag it).

    Do not delete any other exports. Verify the file compiles cleanly after the edit.
  </action>
  <verify>
    Run: `cd /Users/alexanderfedin/Projects/hapyy/languages-web-portal && npx tsc --project e2e/tsconfig.json --noEmit`
    Expected: zero errors. Confirm `loadExampleAndExecute` no longer appears in helpers.ts via grep.
  </verify>
  <done>
    helpers.ts contains no `loadExampleAndExecute` definition, no `DemoPage` import, and TypeScript compilation passes with zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Docker guard to playwright.config.ts and POM contract comment</name>
  <files>playwright.config.ts</files>
  <action>
    Add a top-level guard block immediately after the imports (before `export default defineConfig({...})`). The guard throws synchronously when `E2E_BASE_URL` is not set, so the Playwright run aborts before any test executes:

    ```typescript
    // Docker guard — abort immediately if E2E_BASE_URL is not set.
    // All e2e tests require a running target server (Docker production container or
    // local dev server started manually). Set the env var before running tests:
    //   export E2E_BASE_URL=http://localhost:3000
    if (!process.env.E2E_BASE_URL) {
      throw new Error(
        'E2E_BASE_URL is not set. Tests require a running target server.\n' +
        'Start the target server and then run:\n' +
        '  export E2E_BASE_URL=http://localhost:3000',
      );
    }
    ```

    Remove or update the existing `webServer` block since the guard makes the dev-server auto-start logic moot — the guard enforces that callers always set `E2E_BASE_URL` explicitly. Set `webServer: undefined` (remove the conditional entirely).

    Also update the existing JSDoc comment at the top to remove the sentence about automatic dev-server start, and add a 1-2 line POM contract comment near the `projects:` array:

    ```typescript
    // POM contract: all test interactions with page UI must go through Page Object Models
    // in e2e/pages/. Never use raw page.locator() or page.getByTestId() in spec files.
    projects: [
    ```
  </action>
  <verify>
    Run without the env var: `cd /Users/alexanderfedin/Projects/hapyy/languages-web-portal && E2E_BASE_URL= npx playwright test --list 2>&1 | head -20`
    Expected: error output containing "E2E_BASE_URL is not set" before any test is listed.

    Run with the env var: `E2E_BASE_URL=http://localhost:3000 npx playwright test --list 2>&1 | head -5`
    Expected: test list output (no error).
  </verify>
  <done>
    Running `npx playwright test` without E2E_BASE_URL set produces the actionable error message. Running with E2E_BASE_URL set proceeds normally. The POM contract comment appears near the `projects:` array in playwright.config.ts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add landingContainer to LandingPage POM and fix browser-navigation.spec.ts</name>
  <files>
    e2e/pages/LandingPage.ts
    e2e/tests/browser-navigation.spec.ts
  </files>
  <action>
    **Step A — Update LandingPage POM (e2e/pages/LandingPage.ts):**

    Add a `landingContainer` locator to the class for `data-testid="landing-page"` and a `goto()` wrapper that also waits for the container to be visible:

    ```typescript
    readonly landingContainer: Locator;
    ```

    In the constructor:
    ```typescript
    this.landingContainer = page.getByTestId('landing-page');
    ```

    Add a `waitForVisible()` helper method:
    ```typescript
    async waitForVisible(timeout = 10_000): Promise<void> {
      await this.landingContainer.waitFor({ state: 'visible', timeout });
    }
    ```

    The existing `goto()` method stays as-is (it already calls `page.goto('/')`).

    **Step B — Rewrite browser-navigation.spec.ts to use LandingPage POM:**

    Import `LandingPage` at the top (it is not currently imported).

    Replace every occurrence of:
    - `await page.goto('/')` → `await landing.goto()`
    - `await expect(page.getByTestId('landing-page')).toBeVisible({ timeout: 10_000 })` → `await landing.waitForVisible()`

    Instantiate `landing` inside each test:
    ```typescript
    const landing = new LandingPage(page);
    ```

    The `page.goto('/demo')` and `page.goto('/demo?tool=...')` calls are navigating to the demo page (not the landing page) and are not landing page interactions — these stay as raw `page.goto()` calls since there is no DemoPage.goto() that accepts a URL with params, and the DemoPage POM is already instantiated in tests that need it.

    All `await expect(page.getByTestId('landing-page'))` assertions must be replaced. Do not leave any raw `page.getByTestId('landing-page')` calls in the spec.
  </action>
  <verify>
    Run TypeScript check: `cd /Users/alexanderfedin/Projects/hapyy/languages-web-portal && npx tsc --project e2e/tsconfig.json --noEmit`
    Expected: zero errors.

    Grep for remaining raw `page.getByTestId('landing-page')` in browser-navigation.spec.ts:
    `grep "page.getByTestId('landing-page')" e2e/tests/browser-navigation.spec.ts`
    Expected: no output.

    Grep for `LandingPage` import in browser-navigation.spec.ts:
    `grep "LandingPage" e2e/tests/browser-navigation.spec.ts`
    Expected: import line present.
  </verify>
  <done>
    LandingPage POM has `landingContainer` locator and `waitForVisible()` method. browser-navigation.spec.ts imports and uses LandingPage for all landing page assertions. TypeScript compilation passes with zero errors.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --project e2e/tsconfig.json --noEmit` — zero errors
2. `grep "loadExampleAndExecute" e2e/fixtures/helpers.ts` — no output
3. `grep "DemoPage" e2e/fixtures/helpers.ts` — no output
4. `grep "landingContainer" e2e/pages/LandingPage.ts` — present
5. `grep "page.getByTestId('landing-page')" e2e/tests/browser-navigation.spec.ts` — no output
6. `E2E_BASE_URL= npx playwright test --list 2>&1 | grep "E2E_BASE_URL"` — error message present
</verification>

<success_criteria>
- `loadExampleAndExecute` and its `DemoPage` import are removed from helpers.ts
- Running `npx playwright test` without `E2E_BASE_URL` aborts with: "E2E_BASE_URL is not set. Tests require a running target server. ... export E2E_BASE_URL=http://localhost:3000"
- `LandingPage` POM has `landingContainer` (data-testid="landing-page") and `waitForVisible()` method
- `browser-navigation.spec.ts` uses `LandingPage` for all landing page interactions — no raw `page.getByTestId('landing-page')` calls remain
- TypeScript compiles with zero errors across the e2e project
</success_criteria>

<output>
After completion, create `.planning/phases/19-test-infrastructure-cleanup/19-01-SUMMARY.md`
</output>
