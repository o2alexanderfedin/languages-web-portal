---
phase: 14-execution-flow-e2e-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/pages/ExecutionPage.ts
  - e2e/tests/execution-flow.spec.ts
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02

must_haves:
  truths:
    - "ExecutionPage POM encapsulates all execution-panel locators and actions"
    - "execution-flow.spec.ts replaces java-fv-execution.spec.ts (old file deleted)"
    - "SSE streaming output is verified: console grows incrementally before completion"
    - "Progress indicators verified: button shows 'Running...', connection badge shows CONNECTING/CONNECTED"
    - "All happy-path and streaming tests are Chromium desktop only (serial, Docker)"
  artifacts:
    - path: "e2e/pages/ExecutionPage.ts"
      provides: "ExecutionPage POM with locators and action methods"
      exports: ["ExecutionPage"]
    - path: "e2e/tests/execution-flow.spec.ts"
      provides: "Happy path + streaming + progress E2E tests"
      min_lines: 120
  key_links:
    - from: "e2e/tests/execution-flow.spec.ts"
      to: "e2e/pages/ExecutionPage.ts"
      via: "import { ExecutionPage }"
      pattern: "import.*ExecutionPage"
    - from: "e2e/pages/ExecutionPage.ts"
      to: "e2e/fixtures/helpers.ts"
      via: "re-exports or uses waitForExecutionComplete internally"
      pattern: "waitForExecutionComplete|completed|exit code"
---

<objective>
Create the ExecutionPage POM and migrate the existing java-fv-execution.spec.ts into a new execution-flow.spec.ts covering SSE streaming output (EXEC-01) and progress indicators (EXEC-02).

Purpose: Establish ExecutionPage as the canonical POM for all execution tests (consistent with LandingPage, UploadPage), and migrate the existing happy-path/streaming/progress tests into the Phase 14 structure.
Output: ExecutionPage.ts (POM), execution-flow.spec.ts (Chromium-desktop Docker tests), java-fv-execution.spec.ts deleted.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@e2e/pages/DemoPage.ts
@e2e/pages/UploadPage.ts
@e2e/fixtures/helpers.ts
@e2e/tests/java-fv-execution.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExecutionPage POM</name>
  <files>e2e/pages/ExecutionPage.ts</files>
  <action>
Create e2e/pages/ExecutionPage.ts following the exact UploadPage.ts pattern (typed readonly locators, JSDoc comments, async methods returning Promise&lt;void&gt; or typed values).

Locators to expose (all via getByTestId or role-based):
- `executionPanel`: page.getByTestId('execution-panel')
- `executeButton`: page.getByTestId('execute-button')
- `consoleOutput`: page.getByTestId('console-output')
- `outputPanel`: page.getByTestId('output-panel')
- `toolPicker`: page.getByTestId('tool-picker')
- `exampleSelector`: page.getByTestId('example-selector')
- `exampleDropdown`: page.getByTestId('example-dropdown')
- `loadExampleButton`: page.getByTestId('load-example-button')
- `connectionBadge`: page.locator('.bg-yellow-100, .bg-green-100').filter({ hasText: /CONNECTING|CONNECTED/i }) — for progress indicator assertion
- `statusBadge`: page.locator('.bg-green-100.text-green-800, .dark\\:bg-green-900').filter({ hasText: /COMPLETED/i })

Methods to implement:
- `goto(params?: string): Promise&lt;void&gt;` — navigates to /demo with optional query string
- `selectTool(toolId: string): Promise&lt;void&gt;` — clicks page.getByTestId(`tool-option-${toolId}`)
- `selectExample(exampleName: string): Promise&lt;void&gt;` — selects from exampleDropdown by label
- `loadExample(exampleName: string): Promise&lt;void&gt;` — selectExample + click loadExampleButton + wait for executeButton enabled (10s timeout)
- `execute(): Promise&lt;void&gt;` — clicks executeButton
- `waitForExecutionComplete(timeout?: number): Promise&lt;void&gt;` — polls console-output for /completed|exit code/i (default 180_000ms), mirrors existing helper
- `getConsoleText(): Promise&lt;string&gt;` — returns consoleOutput.textContent() ?? ''
- `isScrolledToBottom(): Promise&lt;boolean&gt;` — page.evaluate on console-output element, checks scrollTop + clientHeight >= scrollHeight - 10

Do NOT import from helpers.ts in ExecutionPage — methods are self-contained in the class. The helpers.ts `loadExampleAndExecute` and `waitForExecutionComplete` functions remain in helpers.ts for backward compatibility with other specs during migration; they do not need to be changed.
  </action>
  <verify>npx tsc --noEmit --project /Users/alexanderfedin/Projects/hapyy/languages-web-portal/e2e/tsconfig.json 2>&amp;1 | head -20</verify>
  <done>ExecutionPage.ts compiles without TypeScript errors; all locators and methods present</done>
</task>

<task type="auto">
  <name>Task 2: Migrate execution tests to execution-flow.spec.ts and delete old file</name>
  <files>e2e/tests/execution-flow.spec.ts</files>
  <action>
Create e2e/tests/execution-flow.spec.ts that replaces java-fv-execution.spec.ts. Import ExecutionPage from '../pages/ExecutionPage'. Do NOT import DemoPage or the loadExampleAndExecute helper — use ExecutionPage methods directly.

Apply these test-suite settings at the top of the describe block:
- `test.setTimeout(180_000)` — all Docker tests are slow
- `test.describe.configure({ mode: 'serial' })` — avoid Docker resource contention
- `test.skip(({ isMobile }) => isMobile, 'Execution tests run on desktop only')` — mobile skip
- Add a `test.use({ ...devices })` or project filter comment noting these run on Chromium desktop via playwright.config.ts project naming (chromium tests match "desktop-chromium")

Migrate all 8 tests from java-fv-execution.spec.ts verbatim, replacing DemoPage/helper usage with ExecutionPage:

**Happy Path tests (keep Java FV framing):**
1. 'bank-account-records example executes and shows VERIFIED'
   - `const exec = new ExecutionPage(page); await exec.goto('tool=java-verification'); await exec.loadExample('bank-account-records'); await exec.execute();`
   - await expect(exec.executeButton).toContainText(/Running/i, { timeout: 10_000 })
   - await exec.consoleOutput.waitFor({ state: 'visible', timeout: 30_000 })
   - await expect(exec.consoleOutput).not.toBeEmpty()
   - await exec.waitForExecutionComplete()
   - assert consoleText matches /VERIFIED|verified|precondition|verification|Z3|successful/i
   - await expect(exec.statusBadge).toBeVisible({ timeout: 5_000 })

2. 'shape-matching example executes successfully'
   - Same pattern, assert /VERIFIED|verified|precondition|verification|Z3|successful/i

3. 'payment-types example shows verification failures for UnsafeRefund.java'
   - Assert /FAILED|failed|VERIFICATION FAILED/i and at least one of the failure keywords

**Streaming verification tests:**
4. 'streaming output shows early markers before final result'
   - Execute, wait for first console content (waitForFunction on console-output textContent length > 0)
   - Snapshot earlyLength, call waitForExecutionComplete, assert finalText.length > earlyLength

5. 'auto-scroll behavior during streaming'
   - Execute, wait for first content, call exec.isScrolledToBottom() → expect true
   - waitForExecutionComplete, call exec.isScrolledToBottom() again → expect true

**Progress indicator tests:**
6. 'loading indicator visible during execution'
   - Execute, await expect(exec.executeButton).toContainText(/Running/i, { timeout: 5_000 })
   - await expect(exec.connectionBadge).toBeVisible({ timeout: 10_000 })
   - After waitForExecutionComplete: buttonText must NOT match /Running/i, MUST match /Java Verification|Run Again/i

**Output file tree tests:**
7. 'output file tree appears after successful execution'
   - After completion: assert page.getByRole('heading', { name: /Output Files/i, level: 3 }) visible
   - Assert exec.outputPanel visible
   - Assert treeitem or text.length > 100 via page.evaluate

8. 'output file tree contains verification artifacts'
   - After completion: exec.outputPanel textContent length > 50 and matches /\.java|\.txt|\.log|Account|Transaction|Shape/i

After creating execution-flow.spec.ts, delete the old file:
`fs.unlinkSync` via Bash: `rm e2e/tests/java-fv-execution.spec.ts`
  </action>
  <verify>cd /Users/alexanderfedin/Projects/hapyy/languages-web-portal &amp;&amp; npx tsc --noEmit --project e2e/tsconfig.json 2>&amp;1 | head -20 &amp;&amp; ls e2e/tests/ | grep -E "execution"</verify>
  <done>execution-flow.spec.ts exists and compiles; java-fv-execution.spec.ts no longer exists</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project e2e/tsconfig.json` exits 0
- `ls e2e/pages/ExecutionPage.ts` exists
- `ls e2e/tests/execution-flow.spec.ts` exists
- `ls e2e/tests/java-fv-execution.spec.ts` does NOT exist (file deleted)
- `grep -c "ExecutionPage" e2e/tests/execution-flow.spec.ts` returns > 0
- `grep -c "DemoPage\|loadExampleAndExecute" e2e/tests/execution-flow.spec.ts` returns 0
</verification>

<success_criteria>
- ExecutionPage.ts exists with all locators and methods; no TypeScript errors
- execution-flow.spec.ts contains all 8 migrated tests using ExecutionPage
- java-fv-execution.spec.ts deleted
- Chromium desktop serial Docker tests correctly structured (test.setTimeout, test.describe.configure serial, isMobile skip)
</success_criteria>

<output>
After completion, create `.planning/phases/14-execution-flow-e2e-tests/14-01-SUMMARY.md`
</output>
