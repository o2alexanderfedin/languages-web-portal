---
phase: 09-tool-activation-examples
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/constants/tools.ts
  - packages/server/src/config/toolRegistry.ts
autonomous: true

must_haves:
  truths:
    - "Java Verification tool shows 'Available' badge on landing page"
    - "Java Verification tool description says 'Prove your Java code is correct'"
    - "Java Verification tool is selectable and executable on the demo page"
    - "Java Verification execution has 120-second timeout"
  artifacts:
    - path: "packages/shared/src/constants/tools.ts"
      provides: "Java verification tool with status 'available' and updated description"
      contains: "status: 'available'"
    - path: "packages/server/src/config/toolRegistry.ts"
      provides: "Java verification tool with available: true and 120s timeout"
      contains: "available: true"
  key_links:
    - from: "packages/shared/src/constants/tools.ts"
      to: "packages/client/src/features/landing/ToolComparisonGrid.tsx"
      via: "TOOLS constant import"
      pattern: "status.*available"
    - from: "packages/server/src/config/toolRegistry.ts"
      to: "packages/server/src/services/executionService.ts"
      via: "getToolConfig lookup"
      pattern: "available.*true"
---

<objective>
Activate the Java Verification tool in the portal by flipping its status from 'in-development' to 'available' and updating its description and execution timeout.

Purpose: This is the switch that makes Java Verification the first live tool in the portal. Without this, users see "In Development" badge and cannot execute the tool.

Output: Updated tool definitions in shared constants and server registry.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tool-activation-examples/09-CONTEXT.md
@packages/shared/src/constants/tools.ts
@packages/shared/src/types/tool.ts
@packages/server/src/config/toolRegistry.ts
@packages/server/src/services/executionService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Java Verification tool definition and registry</name>
  <files>packages/shared/src/constants/tools.ts, packages/server/src/config/toolRegistry.ts</files>
  <action>
In `packages/shared/src/constants/tools.ts`:
- Change the `java-verification` entry's `status` from `'in-development'` to `'available'`
- Change `description` from `'Formal verification for Java code'` to `'Prove your Java code is correct — automated formal verification for modern Java'`
- Keep all other fields unchanged (id, name, category, sourceLanguage)

In `packages/server/src/config/toolRegistry.ts`:
- Change the `java-verification` entry's `available` from `false` to `true`
- Change `maxExecutionTimeMs` from `60000` to `120000` (120 seconds per user decision for Java FV custom timeout)
- Keep all other fields unchanged (id, command, defaultArgs)

Do NOT change any other tool entries.
  </action>
  <verify>
Run `npm run build` from monorepo root to confirm shared package compiles. Run `npm run test -- --run` in packages/server to confirm existing tests still pass. Grep for `available.*true` in toolRegistry.ts to confirm exactly one tool is available.
  </verify>
  <done>
- `tools.ts` has java-verification with status 'available' and benefit-focused description
- `toolRegistry.ts` has java-verification with available: true and maxExecutionTimeMs: 120000
- All existing tests pass
- Build succeeds
  </done>
</task>

</tasks>

<verification>
- Landing page tool grid will show Java Verification with green "Available" badge
- Demo page tool picker will show Java Verification as selectable (not grayed out)
- ExecutionService will accept java-verification tool execution requests (available: true check passes)
- Execution timeout is 120 seconds for Java FV (not default 60)
</verification>

<success_criteria>
- Java Verification status is 'available' in shared constants
- Java Verification description is benefit-focused ("Prove your Java code is correct")
- Java Verification available is true in server registry
- Java Verification timeout is 120000ms in server registry
- Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-tool-activation-examples/09-01-SUMMARY.md`
</output>
